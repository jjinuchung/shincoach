# 신코치 (shincoach) 개발 진행 기록

> 새 세션 시작 시 이 파일과 `.task-memory/` 3종을 먼저 읽는다.

## 2026-09-12

### 진행 내용
- 요구사항 논의 → 로컬 mp4+srt/smi, 순수 HTML/JS PWA, IndexedDB 저장, GitHub Pages 배포로 결정
- 1~6단계 초안 구현: 골격, srt 파서, 가져오기/라이브러리, 플레이어(문장 이동·반복·속도·이중 자막·섀도잉·단어 하이라이트·이어보기)
- /codex 1차 리뷰 14건 중 P1 5건 + 파서 3건 반영 (백그라운드 일시정지, 탐색 동기화, ended 처리, 재생 오류 안내, SW 캐시 범위, 공백 구분줄/VTT ID, 한글 중복, 겹침 병합)
- SMI(SAMI) 자막 지원 (영·한 한 파일 자동 분리, CP949)
- 실제 영화 준비: ffmpeg 설치, mkv 내장 영어 자막 추출 3편, 720p mp4 변환(토이스토리5·미니언즈·모아나), 싱크 검증 도구
- GitHub Pages 배포(v1~v8), Tailscale 파일 서버로 집 노트북에 전송, 태블릿 설치
- 구형 안드로이드(삼성 인터넷 → Chrome 전환) 호환: 문법 하향, FileReader, 오류 배너
- 듣기 먼저 모드(영어 숨기고 N번 듣고 공개), 단어 패널(내장 사전 645→1,436단어 + 280표현), 반복 기본 ∞·설정 유지·섀도잉 순환

### 변경 파일
- `index.html`, `css/style.css`, `js/{app,db,srt,sami,library,player,vocab}.js`, `sw.js`, `manifest.webmanifest`, `vocab/*.json`, `tools/{synccheck.mjs,fileserver.py,vocab_extract.mjs,vocab_scan.mjs}`, `tests/*`

### 결정사항 / 메모
- 사무실 PC의 TRADEMY API가 상시 실행 → ffmpeg 등 무거운 작업은 Idle 우선순위 (CPU 100%로 폰 앱 타임아웃 사고)
- 미니언즈 영어 자막은 CC라 효과음 제거 로직 추가; 한글 smi의 두 화자 동일 대사는 한 줄로

## 2026-09-13

### 진행 내용
- ⚙ 설정 비밀번호(해시 보관), 듣기 먼저 4번 옵션, 기기 진단(마이크·음성 인식 테스트, 녹음 들어보기)
- 🎤 말하기 확인: 문장 끝 → 마이크 녹음 → Web Speech 단어 일치율(폴백: 말소리 길이) → 통과 전 다음 문장 차단, 3번 미달 시 통과, 마이크 불가 시 확인 없이 진행
- 📊 학습 기록 대시보드(부모, 비밀번호): DB v2(sentenceStats/sessions/daily/vocabViews), 주간 요약, 콘텐츠 진행률, 어려운 문장 TOP10(탭→이동), 복습 단어장, 세션, JSON 백업/복원. 아이 화면엔 ⭐정복·오늘의 목표 칩
- /codex 2차 리뷰 10건 전부 반영 (자동 이동 상태 초기화, 말하기 확인 수명주기, DB 업그레이드 차단 안내, 마이크 해제, 소음 바닥 최솟값, 백업 병합, 저장 재시도, 날짜 전환, 공개 시 완료 기록, 긴 문장 내용어 기준)
- v12 배포에 heredoc 백슬래시 깨짐 문법 오류 → v13 즉시 복구, 커밋 전 검사 스크립트(`tools/check.mjs`, `npm test`에 포함) 도입
- `.task-memory/`를 공개 저장소에서 제외 (비밀번호 메모 포함)
- 배포 v9 → v16

### 변경 파일
- `js/{speak,track,stats,diag}.js` 신규, `js/{player,db,app}.js`, `index.html`, `css/style.css`, `sw.js`(v16), `tools/check.mjs`, `tests/{speak,track,stats,db.merge,player.logic,vocab}.test.js` (총 74개)

### 결정사항 / 메모
- 태블릿 = 갤럭시탭 A7(Android 10) + Chrome 152. 처음엔 삼성 인터넷으로 열려 음성 인식 불가 → Chrome으로 재설치. 마이크 정상이지만 음량 작음(RMS 최대 0.03) → 말소리 문턱 상한 0.022
- 말하기 판정: 40% 일치 또는 (2단어+내용어, 6단어 이하) 또는 (3단어+내용어). 3번 미달 시 통과 (사용자 결정)
- 학습 기록 "완료" = 영어 공개 상태로 끝까지 들음(듣기 먼저면 공개 시점). ⭐ = 발음 80%↑. 📊는 부모만(비밀번호)
- Bash heredoc은 백슬래시를 깨뜨림 → 코드는 Write/Edit로, 푸시는 `npm test &&` 체인으로만

### TODO (다음 작업)
- [ ] [백로그] 태블릿 Web Speech 인식 결과 없음 원인 확정 — ⚙ 진단 "단계: start → …" 줄 확인
- [ ] 며칠 실사용 후 📊 기록 보고 판정 기준·표시 조정 (말하기 통과율, 어려운 문장 기준)
- [ ] 태블릿에서 v16 실사용 피드백 (말하기 확인 흐름, 오늘의 목표, ⭐)
- [ ] [보류] 아이 발음 녹음 보관·비교 듣기 (제안 4)
- [ ] 새 영화 추가 시 `tools/vocab_extract.mjs`로 사전 확장, README 절차 참고

## 2026-09-14

### 진행 내용
- 🧩 문장 퍼즐 게임 (v18~v20): 10문장마다 배운 문장의 단어를 섞어 드래그/탭으로 어순 맞추기. 단어 자리 고정(밀림 방지), ⚙ "지금 퍼즐 해보기", 정답 뒤 문장 한 번 더 듣기, 기록(puzzles/puzzleSolved) + 📊 KPI
- 따라 말하기 영어 숨김 3단계 (v19): 처음 숨김 → 못 하면 홀수 번째 단어 빈칸 → 그래도 못 하면 전부
- 🎮 포켓몬 캐릭터 (v21): `js/pokemon.js` 30마리 명단, ⚙ "받기"로 PokeAPI 공식 일러스트 → 256px → IndexedDB(characters, DB v3). 퍼즐 자리 = 말풍선+캐릭터 카드, 캐릭터 눌러도 단어 오감
- ⚡ 경험치·레벨·🎯 몬스터볼 잡기·🎒 도감 (v22): `js/xp.js`(규칙+프로필, DB v4 profile, 백업 포함), `js/catch.js`(볼 던지기 CSS 연출, 운 판정), `js/pokedex.js`(도감: 실루엣/???). 퍼즐 정답 30/20/10 XP, 문장 완료 +2, 말하기 통과 +3(⭐+5), 레벨 Lv1→2 100 이후 +40. 희귀도 4단계 기본 45/30/15/3% × (1+레벨×5%), 최대 90%. 정답마다 던지기 1회, 또 잡으면 +10
- 개발 편의: `tools/devserver.py`(no-store), `?nosw=1`(SW 등록 안 함)

### 변경 파일
- 신규 `js/{puzzle,pokemon,xp,catch,pokedex}.js`, `tests/{puzzle,pokemon,xp}.test.js`, `tools/devserver.py`
- `js/{player,db,track,stats,app}.js`, `index.html`, `css/style.css`, `sw.js`(v22)

### 결정사항 / 메모
- 퍼즐은 문장 전환 시점에 출제(말하기 확인과 안 겹치게), 단어 조각은 구두점·대문자 그대로(힌트)
- 포켓몬 그림은 저작권 때문에 저장소에 두지 않고 기기에서만 보관
- 헤드리스 테스트: localhost SW가 옛 파일을 줌 → devserver + ?nosw + 컨텍스트 재생성(viewport --scale 2→1)
- Write/Edit 도구는 유니코드 이스케이프를 실제 문자로 바꿔 저장 (정규식 동작 동일)

### TODO (다음 작업)
- [ ] 태블릿: v22 새로고침 → ⚙ 캐릭터 받기 → 퍼즐·잡기·도감 실사용 확인
- [ ] 아이 반응 보고 확률/XP 조절 (⚙에서 조절 가능하게 할지), 명단 확장
- [ ] 📊 대시보드에 레벨/퍼즐 그래프

## 2026-09-14 (이어서 — 태블릿 피드백·동기 시스템·리뷰 반영, v23~v28)

### 진행 내용
- 퍼즐 조작 피드백 (v23·v24): 정답 칸에서 단어를 빼면 **빈 자리(점선)** 가 남고 다음 단어가 거기로 들어감 / 드래그가 중간에 끊기던 원인 = 끌고 있는 조각을 DOM에서 실시간 이동 → 포인터 캡처 상실. 조각은 제자리(ghost)+복제본만 이동+자리표시(drop) 미리보기, 이벤트는 document 리스너로 재작성
- 🔊 효과음(Web Audio 합성)·📳 진동·🎊 종이가루·두근두근 무대 흔들림, ⚙ 효과음/진동 토글 (v25)
- 동기 시스템 3종 (v26): 🔥 연속 학습일 스트릭(하루 5문장, 보너스 10~40 XP) / 🔒 레벨 마일스톤(명단 60마리, Lv5·10·15에 10마리씩 해금, 도감 잠금 표시) / 🎯 오늘 목표 달성 +30
- /codex 건설적 리뷰 → P1 1·P2 6·P3 제안. 6건+2건 반영 (v27): 프로필 **증분 저장**(두 창 동시 실행 안전, `applyProfileDelta`), 화면 꺼진 동안 잡기·퍼즐 자동 종료 보류, 마지막 문장 퍼즐, 자정 스트릭 재계산, 목표 보너스 하루 1회(`goalRewarded`), XP 지급 시 기록 즉시 flush, 오늘 첫 퍼즐 5문장, 다음 해금까지 남은 XP 표시
- 부족한 캐릭터 자동 받기 (v28): 앱 시작·online 이벤트 때 명단보다 적으면 조용히 받아옴
- gstack 1.79 → 1.84.1 업그레이드

### 변경 파일
- 신규 `js/sfx.js`, `js/{puzzle,catch,xp,pokemon,pokedex,player,db,track}.js`, `index.html`, `css/style.css`, `sw.js`(v28), `tests/*` (총 111개)

### 결정사항 / 메모
- 드래그 중 캡처된 요소를 `insertBefore`로 옮기면 Chrome이 캡처를 놓침 → 절대 옮기지 말 것 (자리표시만 이동)
- 프로필(XP·잡은 포켓몬)은 통째로 put하지 않고 증분을 한 트랜잭션에서 더함 — 홈 화면 앱+Chrome 탭 동시 실행 대비
- 오버레이 자동 종료는 `document.hidden`이면 보류(`pendingAuto`) → 잠긴 태블릿에서 소리 안 남
- Codex 실행: Windows에서 큰 프롬프트는 `codex exec -` + stdin (인자 길이 제한 exit 126)
- 명단을 늘릴 때 `RARITY_IDS`에도 등급 추가 (테스트가 전부 등급 있는지 검사)

### TODO (다음 작업)
- [ ] 태블릿 실사용: v28 → 드래그 매끄러운지, 잡기(소리·진동·종이가루), 도감, 자동 받기 60마리
- [ ] Codex #7: 가로 모드·큰 글자에서 카드가 스크롤되면 드래그로 정답 칸까지 못 감 → 가장자리 자동 스크롤 (실기기 확인 후)
- [ ] 아이 반응 보고: 전설 실패 누적 보상, 퍼즐 XP 비중, 첫 시도 콤보, 📊에 레벨/퍼즐 그래프

## 2026-09-14 (이어서 — 아이템 1·2단계·부모 모드, v29~v31)

### 진행 내용
- 🛒 아이템 1단계 (v29): 💰 코인(학습 행동으로만 획득: 문장 1·말하기 2/⭐3·퍼즐 5/3/2·목표 10·스트릭 5×일·레벨업 🎁 상자) + 🛒 상점(🎀 장식 10종 개수 보유, 🎨 염색약 7종 소모품·원래 색 무료) + 도감에서 포켓몬 탭 → 장착/염색 모달 → 퍼즐 카드·잡기·도감에 반영(이모지 오버레이 + CSS filter)
- 👨‍👩‍👦 부모 모드 (v30): ⚙(비밀번호) 토글. 반복·듣기 먼저·섀도잉·말하기 확인·퍼즐 없이 연속 재생, 기록·XP·코인 안 쌓임, 상단 칩으로 비밀번호 없이 복귀, 앱 재시작 시 해제. 망나뇽을 전설→희귀로 (아이 지적)
- 🛒 아이템 2단계 (v31): 🤝 파트너(자동 지정, 도감에서 변경) + ❤️ HP 100 — "틀림"이 아니라 "대충 넘김·안 함"(정답 공개 −20·말하기 3번 미달 −10·어제 안 함 −30)에만 깎이고 0이면 😴 쉬는 중(잃지 않음) + 🧪 물약 10/25코인 + 목표 달성 +20 + ⚙ HP 켜기/끄기

### 변경 파일
- 신규 `js/items.js`(코인·카탈로그·HP 규칙·makeFigure), `js/shop.js`(상점·상세 모달), `tests/items.test.js`
- `js/{xp,db,track,player,puzzle,catch,pokedex}.js`, `index.html`, `css/style.css`, `sw.js`(v29→v31), `tests/player.logic.test.js` (총 126개)

### 결정사항 / 메모
- 코인은 XP와 별개 화폐(XP는 레벨·해금). 프로필 백업 병합에서 coins·items·mons·partner는 최근 저장 쪽 통째로(max면 옛 백업이 코인을 되살림)
- 이모지는 Android 10 = Emoji 12까지 → 🪙·🩷 대신 💰·💗. ZWJ 조합도 피함
- 염색 필터는 실제 일러스트로 비교해 확정. 밝은 노랑(피카츄)은 빨강이 연함 — 한계
- 파트너가 없는 기존 프로필은 가장 많이 잡은 포켓몬을 자동 파트너로(ensurePartner)

## 2026-09-15 (말하기 인식 우선·여행 길·배틀, v32~v36)

### 진행 내용
- 태블릿 "대충 말해도 통과" 원인 확정: 안드로이드 Chrome은 getUserMedia 스트림이 열려 있으면 음성 인식이 소리를 못 받음(진단 "동시 사용 테스트"로 재현). 그동안 소리 길이만으로 판정됐음
- v32: 인식 실패 이유를 결과 화면에 표시(🎙 인식 안 됨(이유)) + ⚙ 진단 동시 사용 테스트 / v33: speak.js를 **인식 우선**(releaseMic 후 인식만, 말소리는 인식 이벤트) + 안 되면 에너지 폴백으로 재구성 → 태블릿에서 단어 표시 확인됨
- 🗺️ 여행 길 (v34): 진행 바 → 🏠→🏁 길, 풍경 6~10개(콘텐츠 id 시드), 파트너 포켓몬이 문장 위치에서 걸어감, 도착 시 🎊 + ⚡50 💰20(콘텐츠당 1회)
- ⚔️ 트레이너 배틀 (v35): 문장 완료 시 3%(오늘 5문장↑, 하루 1번) → 다음 전환에서 등장(수락/거절). 공격 = 문장 듣고 따라 말하기 → 등급별 데미지(강한 40/25/8/0, 확실한 28/22/15/5), 상대 15~25, 물약 턴. 승 = 상대 포켓몬 획득 + ⚡40 💰15, 패 = 패배 +1, **3번 누적 시 1마리 잃음**(사용자 결정), 파트너 제외. ⚙ 배틀 연습
- /codex 건설적 리뷰(8건) 중 6건 반영 (v36): 결과 화면에서 닫아도 승패 보존 / 화면 꺼짐 시 턴 무효 / 마지막 문장 섀도잉 뒤 배틀 / 뒤 화면 inert + 연습 진입 가드 / 듣기 건너뛰기·다 말했어요 라벨 + 소리 피드백 / 테스트 보강
- gstack 1.84.1 → 1.87.0

### 변경 파일
- 신규 `js/battle.js`(규칙·타입 60마리·UI), `tests/battle.test.js`, `tests/speak.flow.test.js`
- `js/{speak,player,diag,xp,db,track,items,catch,pokedex}.js`, `index.html`, `css/style.css`, `sw.js`(v32→v36), `tests/player.logic.test.js` (총 143개)

### 결정사항 / 메모
- 새 마이크 기능은 인식과 동시에 getUserMedia를 열지 말 것 (안드로이드 제약)
- 배틀의 공격을 학습(따라 말하기)으로 묶은 이유: 운·상성만이면 학습과 분리돼 "이것만 하려는" 위험. 여행 길·배틀·HP 모두 파트너 1마리에 집중
- 배틀 턴의 말하기는 퍼즐의 🔊 재생 메커니즘(state.puzzleCue·playPuzzleSentence) 재사용, 배틀 뒤 continue는 force:true로 같은 전환에서 퍼즐 중복 방지
- Codex 한글 응답이 Windows 파서(CP949)에서 깨짐 → ~/.codex/sessions 로그에서 UTF-8로 복구

### TODO (다음 작업)
- [ ] 태블릿 실사용: ⚙ 배틀 연습(듣기→말하기→데미지 흐름), 여행 길, 파트너 HP·물약·꾸미기, 통과 기준(40%) 체감
- [ ] 며칠 뒤 HP 수치(−20/−10/−30)·배틀 확률(3%·하루 1번) 체감 조정
- [ ] [나중] Codex #2·#5: 두 창 동시 실행 시 패배 누적·하루 1번·HP를 DB 트랜잭션 판정으로
- [ ] 📊 대시보드에 레벨/퍼즐/배틀 그래프, Codex 잔여 #7·#11~#14, 드래그 가장자리 자동 스크롤

## 2026-09-15 ~ 09-16 (학습 3층 + 태블릿 피드백, v37~v48)

### 진행 내용
- **방향 전환**: "보상은 이미 8겹으로 포화, 정작 학습 알맹이가 얇다"고 진단. 문장이 한 번 지나가면 끝나고, 단어 패널은 읽기 전용이며, 발음 피드백이 숫자 하나뿐이라는 세 구멍을 우선순위로 제시 → 사용자가 순서대로 승인
- **🔁 복습 — 간격 반복 (v37)**: `js/review.js` 신규. 라이트너 1·2·4·7·14일 → 👑 졸업. 콘텐츠를 열 때 "오늘의 복습 3문장"(건너뛰기 자유, 하루 2번까지 제안), 한 문장 = 한글 뜻 → 🔊 듣기 → 🎤 따라 말하기. 보상은 **복습에서만 나오는 🌟 황금 몬스터볼**(잡힐 확률 2배, 하루 1개) + ❤️ 파트너 회복 — "코인을 더 주는 것"이 아니라 **가장 센 보상(잡기)의 경로를 복습으로 옮기는** 방식. 어뷰징은 3중 차단(푼 문장은 dueAt이 밀려 그날 재출제 불가 / `daily.reviewGolden`을 날짜 단위 전역으로 두어 영화를 바꿔 열어도 1개 / 백업 병합 OR)
- **🎯 말하기 단어별 피드백 (v38)**: `scoreTranscript`에 `hits` 추가 + `wordResults` 신규. 결과 화면에 단어별 ✅/❌, 못 말한 단어를 누르면 **그 구간만 재생**(`playWord`, `wordTimings` 자리와 1:1). 누르는 동안 자동 진행 타이머가 미뤄짐. `track.missedWords` → 📊 "자주 놓치는 단어"
- **🔤 단어 학습 (v40)**: 단어 패널 탭 → 그 단어·표현만 재생(`vocab.findTokenRange`). 복습 회차에 **뜻 4지선다 1문항**, 단어도 문장과 같은 라이트너 규칙(`vocabViews`에 box·dueAt·quizzes)
- **✍️ 받아쓰기 (v42)**: `js/dictation.js` 신규. 복습의 **세 번째 문항 타입**. 빈칸은 **아이가 못 말했던 단어 우선**(`sentenceStats.missed` 재사용), 오답은 `speak.editDistance`로 **비슷하게 들리는 단어**(through → tough·rough·thought). 타이핑 없이 보기에서 고르기
- **/codex 건설적 리뷰 3회** (복습 9건·단어 피드백 6건·단어 학습 8건) → **전부 반영**. 가장 큰 것들: 복습 box 병합이 max라 실패로 내려간 단계를 옛 백업이 되돌리던 버그 / 단어 클릭이 오버레이로 전파돼 **기능이 실기기에서 아예 동작하지 않던 [P1]** / 사전의 "조심해!"와 "조심해"가 함께 보기로 나와 정답을 알고도 틀리던 [P1] / 표현 범위를 첫·끝 단어로 찾아 문장 전체가 잡히던 [P1]
- **태블릿 실사용 피드백 (v43~v48)**: 설정 select가 빈 것처럼 보이던 버그, ⚙ "말하기 결과 보는 시간"(1~5초) 신설, 따라 말하기 기본 ×2.0, **음성 인식이 중간에 끊기던 진짜 원인**, 두 화자 겹친 자막 제외, 장식 위치 자동 계산 + **드래그로 직접 조정**, 연습·복습 뒤 영상 자리 복원, 캐릭터 8마리씩 이어받기, **명단 100마리**, 희귀도 비율 재조정

### 변경 파일
- 신규 `js/review.js`(복습 규칙+UI), `js/dictation.js`(받아쓰기 규칙), `tests/{review,dictation}.test.js`
- `js/{speak,player,track,db,vocab,items,xp,pokemon,puzzle,catch,shop,stats,diag,battle}.js`, `index.html`, `css/style.css`, `sw.js`(v37→v48)
- `tests/{player.logic,speak,speak.flow,db.merge,items,vocab,pokemon,xp,battle}.test.js` — 143개 → **250개**

### 결정사항 / 메모
- **보상 설계 원칙**: 양(코인 더 주기)이 아니라 **종류**로 차별화. 복습에서만 황금 볼이 나오므로 "여기서만 얻는 것"이 생김. 어뷰징은 막되 "복습을 더 하는 것"은 막지 않음(2회차부터 ⚡·💰만)
- **`state.shadowNext`는 기존 반복 신호**('repeat'|'next')였다. 같은 이름으로 타이머 함수를 덮어써 반복 ∞가 깨졌다 → `state.resultDelay`로 분리. 기존 이름을 재사용하기 전에 grep할 것
- **테스트 스텁의 구조적 한계**: `makeEl()._fire('click')`는 **버블링을 모사하지 않아** `stopPropagation` 누락을 못 잡는다. v38이 이 때문에 "테스트 193개 통과"인 채로 실기기에서 동작하지 않았다 → `clickWithBubble` 헬퍼 추가. vm 컨텍스트 배열은 `Array.from()`으로 복사 후 `deepEqual`
- **음성 인식이 끊기던 진짜 원인**(v46): `network`(구글 서버 순간 불안)와 `start-failed`(앞 인식이 덜 정리된 채 start — 안드로이드에서 흔함)가 SR_FATAL에 있어 **한 번만 나도 즉시 폴백**. 게다가 `no-speech`(아이가 말을 안 함)까지 실패로 셌다. → SR_FATAL은 권한·마이크·서비스만, 나머지는 연속 3회, `no-speech`는 제외, 폴백 대기 3분 → 1분. 다음을 위해 `speakLog()`(최근 12회)를 ⚙ 진단에 노출
- **장식 위치**: 모든 포켓몬이 같은 좌표(가로 중앙·위 -16%)를 써서 라프라스는 나비가 뜨고 파이리는 선글라스가 얼굴을 덮었다. `headAnchor`가 그림의 불투명 픽셀 최상단 + 아래 15% 구간의 가로 중심을 계산(6%면 피카츄 귀 한쪽만 잡혀 0.232, 15%면 0.473). 그래도 안 맞는 건 **아이가 드래그로 옮겨 `mons[id].gearPos`에 저장**
- **명단 확장 시 반드시**: `RARITY_IDS`(등급) + `battle.TYPE_OF`(타입)를 같이 채울 것. 빠지면 조용히 보통·노말이 된다 → 테스트로 고정(타입 누락 0, 비율 흔함>보통>희귀>전설, 전설 12% 이하)
- 포켓몬 한글 이름은 **PokeAPI 한국어 공식 이름으로 대조**했다(기억으로 적었으면 386을 "데오키시스"로 틀렸을 것 — 공식은 "테오키스")
- Codex 프롬프트는 40~50KB로 유지. 90KB는 330초 안에 응답 생성 전 타임아웃

### TODO (다음 작업)
- [ ] 태블릿 실사용: 복습이 실제로 뜨는지(오늘 배운 문장이 내일 due), 받아쓰기 보기 4개가 소리 없이 눈으로 구별되는지, 단어 패널 탭 감도, 장식 드래그 조작감
- [ ] ⚙ 진단의 "최근 말하기 기록"으로 인식이 또 끊기면 원인 확인 (🎤 / 🔇network 같은 형태)
- [ ] 며칠 뒤 조정: 복습 간격(1·2·4·7·14일)이 초4에게 맞는지, 빈칸 2개가 과한지, 말하기 결과 3초가 적당한지
- [ ] [나중] Codex 미반영: 두 창 동시 실행 시 황금 볼·HP·패배 경쟁을 DB 트랜잭션 판정으로 / 화면 꺼짐 중 복습 재시도 타이머 / 복습 회차 Promise 소유권(이전 회차가 새 회차의 speakStop을 지움)
- [ ] 제안했던 나머지: 👨‍👩‍👦 부모→아이 한 줄 메시지(대시보드에서 적으면 아이 화면에 표시), 🌙 마무리 화면(오늘 배운 문장 3개 회상)
- [ ] 📊 대시보드에 레벨·퍼즐·복습 그래프

## 2026-09-16 ~ 09-17 (종료 버튼·에세이·아빠 교정 왕복·포켓몬 게임 확장, v49~v59)

### 진행 내용
- **🚪 종료 버튼 (v49)**: 시작 화면 맨 아래 → 인사 화면(오늘 요약)에서 한 번 더 확인 → `window.close()`. 설치한 PWA는 닫히지만 **브라우저 탭은 close()를 무시**하므로 0.5초 뒤 "홈 버튼으로 나가면 돼요"로 전환
- **포켓몬 영상 2편 자막 제작**: 자막 없는 유튜브 영상(Gengar 14분33초 180문장 / Ash&Pikachu 8분7초 86문장)을 faster-whisper로 인식 → 교정 → 한글 직접 번역. 애매한 27개 구간은 **그 구간만 다시 인식해 대조**. 단어장 +37(1,831→1,868)
- **✍️ 에세이 (v50, 엄마 요청)**: 30분 학습하면 오늘 배운 문장 3개의 뒤를 비워 주고 아이가 자기 이야기로 채움 → **규칙 기반 오프라인 교정** → 위(내가 쓴 글)/아래(신코치가 고친 글) → 따라 읽기. `js/essay.js` 신규
- **/codex 건설적 리뷰 (P1 6 · P2 6) 중 11건 반영** — 과교정을 전부 걷어냄(아래 결정사항)
- **👨‍👩‍👦 아빠 교정 왕복 (v51~v53)**: 규칙으로 못 고치는 어순을 사람이 메운다. 📊에서 아이 글을 복사/사진 → Claude가 교정 → 앱에 되돌림. 태블릿에 메신저가 없어 **교정문을 `coach/fixes.json`에 적어 배포로 전달**하는 길까지 만듦(태블릿에서 입력 0)
- **⚔️ 배틀 확률 3% → 4% (v54)**
- **⭐ 등급 옮기기 (v55·v57)**: 아이가 도감에서 신청 → 📊에서 부모가 승인. 못 잡은 포켓몬도 신청 가능, 일괄 승인
- **🎤 한 번 더 읽기 (v56)**: 통과해도 틀린 단어가 표시된 문장을 보며 한 번 더 정확히 읽기
- **⭐ 메가진화·거다이맥스 (v58)**: 🔑 키스톤 600 + 💠 메가스톤 600 / 🍄 다이버섯 10개 → 🍲 다이스프. 배틀에서 메가 ×1.4, 거다이맥스 ×1.6(3턴). 명단 30마리가 변신
- **🔴 볼 등급 (v59)**: 슈퍼볼 25(×1.5)·하이퍼볼 60(×2)·마스터볼 1,200(확정). 잡기 화면이 볼 고르기로

### 변경 파일
- 신규 `js/essay.js`(에세이 규칙+UI), `coach/fixes.json`+`coach/README.md`(배포로 전달하는 교정문), `tests/essay.test.js`
- `js/{player,xp,items,pokemon,shop,stats,catch,battle,track,db,app}.js`, `index.html`, `css/style.css`, `tools/whisper_srt.py`, `vocab/words.json`, `sw.js`(v48→v59)
- `tests/{player.logic,items,xp,battle,stats,track,essay}.test.js` — 250개 → **289개**

### 결정사항 / 메모
- **규칙 교정의 제1원칙은 "과교정 금지"**: 편집거리로 비슷한 사전 단어를 찾으면 **맞게 쓴 말을 망친다**(soccer→sorcery, goat→goal, **ducks→duck**). 명시적 오타 목록 + 붙은 두 글자 자리바꿈만 남기고, `vocab.stemWord`로 변화형은 항상 보존. 3인칭 s 규칙은 통째로 제거(`it go away`를 망침) — **못 고치는 것보다 맞는 문장을 망치는 게 나쁘다**
- **헤드리스 검증을 건너뛰지 말 것**: 에세이에서 `sfx`를 함수처럼 불러 **제출 버튼이 통째로 죽어 있었는데 테스트 268개는 통과**했다. 오버레이를 스텁으로 바꾸는 테스트는 UI 배선을 검증하지 못한다
- **`res.ratio`는 없는 필드**였다 — `runSpeakCheck`는 `passed`/`score.ratio`를 준다. 이 때문에 성공 효과음이 한 번도 안 울렸다
- **연습 반복은 통계에 넣지 않는다**: 🎤 한 번 더 읽기는 XP·시도·통과 횟수를 안 올리고 bestRatio만 갱신. 통계가 부풀면 부모가 실력을 잘못 읽는다
- **아이 권한 설계**: 등급 옮기기는 곧 잡기 확률이라 **아이는 신청, 부모가 승인**. 반대로 에세이·복습은 아이가 자유롭게
- **경제 기준**: 하루 ≈ 100~130코인. 장식 30~150 / 볼 25~60 / **큰 목표 1,200**(메가 세트·마스터볼) ≈ 열흘 치
- **PokeAPI를 사실 확인에 쓸 것**: 한국어 공식명·변신 폼 id·아이템 설명. 거다이맥스는 "스톤"이 아니라 **다이버섯→다이스프**가 맞다
- **Codex는 영어로 답하게 할 것**: 한글 응답이 Windows CP949 파서에서 깨진다. 영어로 받아 번역하면 세션 로그 복구가 필요 없다
- heredoc은 여전히 백슬래시를 먹는다 — `split(/\r?\n/)`가 실제 개행으로 깨져 파일이 죽었다. **코드는 Write/Edit로**

### TODO (다음 작업)
- [ ] 태블릿 실사용: ✍️ 에세이(초4가 영어 2~5단어 타이핑하는 부담, TTS 영어 음성 유무), 🎤 한 번 더 읽기의 시간 체감, ⭐ 등급 신청·일괄 승인, 🔴 볼 고르기
- [ ] 첫 에세이 교정 왕복 해보기 (📊 사진 → `[번호]` 교정 → `coach/fixes.json` 배포)
- [ ] 며칠 뒤 조정: 메가 세트·마스터볼 1,200이 초4에게 너무 먼지, 🍄 다이버섯 하루 2개가 적당한지, 배틀 4% 체감
- [ ] 진우에게 확인: `라이츄-메가X/Y`·`개굴닌자-메가`가 실제로 있는 폼인지 (PokeAPI에는 있으나 내 지식 기준일 이후 자료)
- [ ] `Touch it, Pikachu!`(Ash 편 1분22초) 실제로 그렇게 들리는지 확인
- [ ] [나중] 두 창 동시 실행 경쟁(황금 볼·HP·패배·에세이 완료 플래그)을 DB 트랜잭션 판정으로 한꺼번에
- [ ] 📊 대시보드에 레벨·퍼즐·복습 그래프 / 🌙 마무리 화면

## 2026-09-17 (🪟 두 창 동시 실행 경쟁 정리, v60)

### 진행 내용
- 백로그에 세 세션째 남아 있던 "두 창 동시 실행 시 황금 볼·HP·패배·에세이 플래그 경쟁"을 **원인이 하나라 한꺼번에** 정리. 뿌리는 `putDaily(t.daily)`처럼 **밖에서 읽어둔 사본으로 통째로 저장**하는 것
- **실제 피해를 브라우저에서 재현**: 예전 방식으로 두 창이 30초·20초를 저장하면 결과가 **20초**(30초 증발), 문장 하나 사라짐, **황금 볼 받은 표시가 false로 풀림**(하루 한 번이 무력화). 보상 중복보다 **공부 기록 소실**이 더 큰 피해였다
- `db.js`에 원자 연산 6종: `applyDailyDelta`(증분) · `claimDailyFlag`/`claimDailyCount`(하루 한 번·N번 선점) · `applyHpChange`(바뀐 만큼 + 클램프 + 물약 소모) · `applyBattleLoss`(누적과 "3번이면 잃음" 판정을 함께) · `applyPurchase`(살 수 있는지 판정) + `editDaily`
- `track.js`: `t.daily`는 보기용, 저장은 `t.dailyDelta`(늘어난 만큼)만. `mark*` 6개가 `Promise<선점 성공 여부>` → `player.js`는 **이긴 창에서만** 보상 지급
- 상점 구매도 같은 구멍이 있었다(두 창에서 1,200코인으로 마스터볼 2개) → `buyItem`·`equipMega`·`makeSoup`가 `applyPurchase`를 쓰고 비동기가 됨
- 부모가 📊에서 교정문을 붙이는 `applyEssayFixes`·`markEssayRead`도 아이 기록을 덮어쓰고 있었다 → `editDaily`로

### 변경 파일
- `js/{db,track,player,xp,shop,review,essay}.js`, `sw.js`(v59→v60)
- `tests/{track,player.logic,items,battle,db.merge}.test.js` — 289개 → **296개**

### 결정사항 / 메모
- **원리 하나**: 판정과 계산을 **트랜잭션 안으로**. IndexedDB는 같은 스토어의 readwrite 트랜잭션을 직렬화하므로, 트랜잭션 안에서 읽고 계산하면 그게 곧 잠금이다
- 트랜잭션 안의 `await`는 **IDB 이벤트에서 resolve되는 Promise일 때만** 살아 있다 (마이크로태스크 안이라 트랜잭션이 아직 활성). 다른 Promise를 기다리면 트랜잭션이 닫힌다
- **`putDaily`/`putProfile`은 앱 코드에서 쓰지 말 것** — 주석으로 못 박음. 백업 가져오기·시험용만
- `mark*`가 Promise를 주므로 **보상은 반드시 `.then(won => ...)` 안에서**. 기존 테스트가 sync라 `await tick()`을 넣어야 했다 — 이 규칙을 어기면 테스트에서 바로 드러난다
- 테스트 스텁의 `daily`를 **날짜별 Map**으로 바꿔, 통째로 덮어쓰는 코드가 다시 들어오면 깨지게 했다
- 헤드리스에서 진짜 IndexedDB로 19개 항목 확인 + **상점 버튼을 실제로 눌러** 확인 (v50에서 "테스트 268개 통과인데 버튼이 죽어 있던" 교훈)

### TODO (다음 작업)
- [ ] 배포 후 태블릿에서 하루 치 학습 — 기록이 정상으로 쌓이는지 (저장 구조가 바뀐 곳)
- [ ] 이전 세션 TODO 그대로: ✍️ 에세이·🎤 한 번 더 읽기·⭐ 등급 신청·🔴 볼 고르기 실사용, 첫 에세이 교정 왕복, 진우에게 메가 폼 확인
- [ ] 📊 대시보드에 레벨·퍼즐·복습 그래프 / 🌙 마무리 화면

## 2026-09-17 (이어서 — 비밀번호·아빠 교정 배포·퍼즐·복습 버그·🎟️ 교환권, v61~v67)

### 진행 내용
- **설정 비밀번호 0715 → 0917**: `SETTINGS_PIN_HASH`(SHA-256)와 **구형 브라우저 폴백(뒤집은 문자열)** 두 곳. 폴백을 빼먹으면 `crypto.subtle`이 없는 브라우저에서 옛 비번이 그대로 통한다
- **👨‍👩‍👦 첫 아빠 교정 왕복 실행**: 📊 사진 3문장을 교정해 `coach/fixes.json`으로 배포. `infront→in front` / `I know right?He's→I know, right? He's` / `My musical instrument **is** in my hand`(동사 누락 — 규칙으로는 못 잡는 딱 그 경우). 최소한만 고쳐 아이가 고른 말은 그대로 뒀다
- **🧩 퍼즐 5단어 이상만 (v62)**: `Use Fire Punch!` 같은 3~4단어 감탄·명령 조각이 퍼즐로 나왔다(아버님 지적). 실제 자막 5,527문장으로 재 보니 후보는 54%→32%로 줄지만, 10문장에 한 번이라 **건너뛰는 차례는 젠가 0%·애쉬 0%·토이스토리 1%**
- **🔁 복습 버그 2건 + 가로 드래그 (v63)**: ① `speakStop`을 회차 확인 **전에** 지워 새 회차의 `다 말했어요 ✓`가 죽던 것(되돌려 재현: 고치기 전 `stop=[]`) ② `sleep()` 6곳에 `document.hidden` 가드가 없어 잠긴 태블릿에서 혼자 넘어가던 것 ③ 가로 모드에서 `touch-action:none` 때문에 조각을 정답 칸까지 못 옮기던 것 → 가장자리 자동 스크롤
- **✍️ 에세이 10단어 이상·쓰는 양 4~8단어 (v64)**: 진우 글이 2단어뿐이었다. **문장만 길게 하면 고정 부분만 길어진다**는 걸 짚고 `MAX_BLANK_WORDS`도 함께 올림. `It's really me! Yes! My goodness...`처럼 **네 문장이 합쳐진 큐**를 `isMultiSentence`로 제외(9단어라 길이로는 안 걸린다)
- **🎟️ 다음 영상 교환권 (v65~v66)**: 포켓몬 영상을 일부러 안 넣어 두신 것을 **보이게 잠그고 벌어서 열게**. 조건 셋(코인 4,000 + 전체 문장 80% + 복습 통과 60). 표지는 장면 캡처가 아니라 **나오는 포켓몬 그림 + 실제 대사 한 줄**(저작물을 저장소에 안 두는 원칙)
- **💰 코인 누수 발견·수정**: ◀▶로 문장을 옮겼다 오면 `resetSentenceState`가 말하기 확인을 다시 켜서 **코인이 무제한**이었다. 코드상 정상 수입은 하루 294~824
- **/simplify 4-에이전트 리뷰**: `sentenceStats`가 없애려던 패턴에 그대로 남아 복습 진도를 잃던 것, 프로필 규칙이 트랜잭션에 갇혀 **테스트가 폴백만 타던 것**(items 11·battle 5건 조용히 삼켜짐) 등 12건 반영, 앱 코드 58줄 순감소
- **/codex 건설적 리뷰 → P1 4건 수정 (v67)**: 아래 결정사항 참조

### 변경 파일
- 신규 `js/unlock.js`(교환권 규칙+카탈로그), `tests/unlock.test.js`
- `js/{db,track,xp,player,review,puzzle,essay,library,stats,pokemon,shop,srt,app}.js`, `css/style.css`, `index.html`, `coach/fixes.json`, `sw.js`(v60→v67)
- `tests/{db.merge,track,player.logic,items,battle,puzzle,essay,srt,unlock}.test.js` — 296개 → **318개**

### 결정사항 / 메모
- **[Codex P1] 영상을 지우면 조건이 저절로 채워졌다** — `deleteItem`은 `sentenceStats`를 안 지운다. 분모는 현재 영상, 분자는 전체 기록이라 어려운 영화를 지우면 그 기록이 분자에 남고 분모에서만 빠진다. **기능의 목적이 삭제 한 번으로 무효.** `unlockState({ itemIds })`로 지금 가진 영상만 센다 (105/80 ready=true → 5/80 ready=false)
- **[Codex P1] 분모가 플레이어와 규칙이 달랐다** — 예고는 항상 합치고 길이 밖 큐도 셌다. `srt.countPlayableCues` 하나로 통일(stats.js도 같이 씀)
- **[Codex P1] 공부하고 돌아와도 숫자가 그대로** — `closePlayer`가 저장을 await 안 하고 `showView`했고 `showView`는 목록을 안 그렸다. 아이에게 가장 실망스러운 평범한 버그
- **[Codex P1] 버튼을 억지로 켜면 조건을 건너뛰었다** — 가격은 카탈로그에서, 중복은 거절, 조건은 `verify()`로 **구매 경로 안에서** 재확인
- **저작물 원칙을 지킨 선택**: 애니 장면 캡처를 커밋하면 "닌텐도 저작물은 공개 저장소에 두지 않는다"가 깨진다(GitHub Pages 무료는 Public만). `pokemon.ensureArt`로 **명단 밖 포켓몬 그림도 PokeAPI에서 받아 기기에만** 둔다. ROSTER 밖 id는 `loadCharacters`가 걸러 도감·퍼즐엔 안 나온다
- **테스트 스텁이 수정을 검증 못 하는 경우** — Codex가 짚었다: `claimSpeakReward: () => true` 스텁 때문에 코인 누수 수정이 테스트되지 않았다. 회귀 테스트는 **순수 함수 쪽에 직접** 붙일 것
- **에세이 끊을 자리**: 뒤에서부터 훑으면 늘 빈칸이 최소가 된다 → 범위 **가운데**에 가장 가까운 자리를 고른다(실측 분포가 6단어에 몰림)
- **다음 방향 결정**: 🔢 수학을 **이 앱의 두 번째 과목**으로 (별도 앱 아님 — 도감·코인을 공유해야 "수학을 해서 마스터볼을 산다"가 성립). 수학은 문제 생성·채점이 자동이라 shincoach에서 제일 힘들었던 두 병목이 없다. 국어는 정답이 하나가 아니라 가장 어렵다(에세이에서 이미 겪음)

### TODO (다음 작업)
- [ ] **태블릿 실사용 — 이게 지금 가장 값어치 있다.** v49~v67(19개 버전)이 실기기에서 거의 검증되지 않았다. 특히 ✍️ 에세이의 🔊 TTS 영어 음성 유무, 어제 배포한 아빠 교정 3문장이 뜨는지, 가로 모드 퍼즐 드래그
- [ ] 설정 비밀번호가 **0917**로 바뀐 것 확인 (0715는 막힘)
- [ ] 🎟️ 교환권 조건(4,000 / 80% / 60개)이 진우에게 너무 먼지 며칠 보고 조정 — `js/unlock.js` 상수 세 개
- [ ] 📊 부모용 추세 화면 (주 단위 말하기 통과율·퍼즐 정답률·⭐정복 추이) — 데이터는 이미 다 쌓여 있고 그리기만 하면 됨
- [ ] [나중] 🔢 수학 과목 추가 — **태블릿 검증이 끝난 뒤에** 시작
- [ ] [Codex P2 미반영] 교환권 배달 취소·환불, 제목이 다를 때 부모가 연결하는 기능, 조건이 구조적으로 불가능할 때 안내, 포스터가 `artUrl`을 안 읽는 것

## 2026-09-17~18 (실기기 신고 대응 이틀, v68~v82)

### 진행 내용
아버님이 태블릿에서 진우를 지켜보며 준 신고를 하나씩 받아 고쳤다. **재현 → 원인 확정 → 고침 → 회귀 테스트** 순서를 지켰고, 추측으로 넘어간 것은 없다.

- **🚑 저장 깨짐 (v68)**: "저장소를 열 수 없어요: Data lost due to missing file"로 앱이 멈춤. 별도 Chrome 프로필로 IndexedDB 곁 파일을 지워 가며 재현 → **Chrome은 64KB가 넘는 값을 곁 파일로 뺀다**(30KB·60KB 인라인, 70KB부터 파일). `items` 레코드는 자막 전문이라 거의 항상 넘고, 그 파일이 사라지면 `items.getAll()`이 **통째로** 실패한다. `getAllKeys` + 한 건씩 읽기로 바꾸고 깨진 것만 `broken`으로 표시. 깨진 영상의 제목은 `sessions.title`에서 되찾음. **아이 기록(profile·daily·sentenceStats)은 작아서 멀쩡했다**
- **🛟 기록 사본 (v70)**: 코인·도감·복습 진도를 localStorage에 자동 사본. 형식을 📊 "가져오기"와 같게 만들어 복구에 병합 규칙을 그대로 씀. 복구는 부모가 📊에서 결정
- **🧩🎀 드래그 먹통 (v70)**: 진우가 "퍼즐이 아무 반응이 없어 앱을 껐다 켜야 한다". 드래그 중 `pointerup`이 안 오면(안드로이드는 화면이 꺼지면 안 준다) `ui.drag`가 남아 이후 모든 터치가 무시됐다. 꾸미기 장식 드래그도 같은 구멍(옛 코드로 되돌려 확인하니 장식이 손가락을 계속 따라다녔다)
- **🎟️ 예고·🎒 도감 중복 (v71·v72)**: "팬텀 광고 문구가 두 번" — 화면을 지우고 `await` 하는 함수에 요청 번호가 없었다. `main()`이 `showView('library')`와 `initLibrary()`로 **매번 두 번** 그렸다. 도감도 같은 모양(🎒를 빠르게 두 번 누르면 내용이 두 배)
- **⚔️ 배틀 8%·하루 3번 (v73·v74)**: "하루 종일 안 나왔다" → 4%에서 하루 30문장이면 못 만날 확률 35%. 계산으로 확인하고 올림
- **🎤 말하기 인식 (v75·v80)**: ① 안드로이드 인식기는 말 시작 전 침묵에서 스스로 끝나는데 그걸로 판정해 버려, 아이가 문장을 읽고 숨 고르는 사이 끝나면 "말소리를 못 들었어요"가 떴다 → 같은 문장에서 2번까지 다시 듣기 ② `prepareMic()` 대기 중 `releaseMic()`이 불리면 늦게 온 스트림이 인식을 막았다 → 세대 번호 ③ **`state.speakUnavailable`이 켜지기만 하고 꺼지는 데가 없었다** — 마이크를 한 번만 못 잡아도 세션 내내 말하기 확인이 사라져 앱을 껐다 켜야 했다 → 시각으로 바꿔 3분 뒤 자동 재시도
- **🔁 복습 (v76)**: 영상 열 때 한 번만 묻고 2번 거절하면 그날 끝이라 밀렸다 → 학습 중 20% + 8문장 쿨다운. ❤️15를 매번 주도록(전에는 첫 회차만)
- **🎟️ 조건을 비율 → 개수 (v77·v78·v79)**: "진도가 안 는 것 같다"가 맞았다. 5,527문장의 80% = 4,422문장이라 하루 40문장을 해도 막대가 0.9%. **영상을 넣으면 목표가 더 멀어지는** 구조이기도 했다. `NEED.doneSentences`(1,000)로 바꾸고 코인 2,000·복습 50으로 조정
- **🎵 이벤트 BGM (v81)**: 퍼즐·배틀·잡기에 8비트 합성 음악. **끝난 자리를 기억해 다음 이벤트에서 이어서**. 말하기 중에는 멈춤
- **🗣 미니언즈어·🔓 자유 이동·🟡 하이퍼볼 (v82)**: 영어가 아닌 대사는 따라 말하라고 안 하고(실측 미니언즈 12%), ⏭ 넘기기 버튼을 넣고, 이미 끝낸 문장으로는 자유롭게 이동하게 하고, 하이퍼볼을 600으로
- **📚 포켓몬 영상 3편**: prime_suspect 65문장 · wild2 46 · iconic 103. 중복 0% 확인 후 제작. 단어장 1,868 → 1,928
- **🎬 모아나 2.24GB**: 이미 H.264인데 태블릿이 거부 → **2GB 초과**가 원인. 1.06GB로 변환

### 변경 파일
- 신규 `js/bgm.js`, `js/backup.js`, `tools/{dupcheck.mjs,align_srt.py,verify_lines.py}`, `tests/backup.test.js`
- `js/{db,track,player,speak,puzzle,shop,library,stats,unlock,vocab,items,sfx,pokedex,battle,catch,app}.js`, `css/style.css`, `index.html`, `vocab/{words,phrases}.json`, `tools/check.mjs`, `sw.js`(v67→v82)
- 테스트 318개 → **350개**

### 결정사항 / 메모
- **"앱을 껐다 켜면 낫는다"는 신고는 모듈 전역 플래그부터 본다** — 이틀에 세 번 같은 모양이었다(퍼즐 `ui.drag`, 말하기 `speakUnavailable`, 예방으로 BGM). 켜는 곳과 끄는 곳의 수를 세면 바로 나온다. 고칠 때는 **시각으로 바꿔 자동 복구**하거나 **다음 사용자 동작이 되돌리게** 한다
- **화면을 지우고 await 하는 함수에는 요청 번호가 필요하다** — 예고·도감 중복이 모두 이 모양이었다. `renderStats`는 await **뒤에** 한 번 더 비워서 무사했다
- **조건은 비율보다 개수** — 비율은 분모가 커지면 아이가 공부해도 진도가 안 늘고, 콘텐츠를 늘려 줄수록 목표가 멀어진다. 개수로 바꾸니 "지워서 조건 채우기" 구멍(Codex P1)도 구조적으로 사라졌다
- **판정 기준은 실제 데이터로 잰다** — 영어 여부 판정을 처음엔 사전 적중률만으로 했다가 `James and Henry!`·`But thankfully Ed intervened,` 같은 멀쩡한 영어가 걸러졌다. 이름(문장 중간 대문자)을 빼고 영어다운 꼬리를 인정한 뒤 **실제 자막 세 편으로 재서** 확정(미니언즈 12% · 토이스토리5 2% · 젠가 20%)
- **단어장에 일부러 넣은 외국어가 판정을 흔들었다** — `bello`가 "안녕!(미니언 말)"로 사전에 있어 영어로 세어졌다. 같은 표시가 47개 → `vocab.foreign`으로 분리. 단어 패널에는 그대로 둔다
- **`tools/check.mjs`의 APP_SHELL 가드가 실제로 일했다** — `js/bgm.js`를 sw.js에 안 넣은 것을 바로 잡아냈다(안 잡혔으면 오프라인에서 BGM만 조용히 죽었을 것)
- **2GB는 안드로이드 Chrome의 실질 한계** — 코덱이 멀쩡해도 안 열린다. 가져오기 안내도 크기를 짚도록 고쳤다
- ffmpeg는 `-map`으로 영상·소리만 골라도 **챕터 트랙을 따라 붙인다** → `-map_chapters -1`

### TODO (다음 작업)
- [ ] **태블릿 실사용 관찰 후 수치 조정** — 🎟️ 1,000문장이 적당한지(📊 "콘텐츠별 진행"의 합으로 확인), 🔁 복습 20%가 잦은지, 🎵 BGM 분위기·볼륨, 🗣 판정 기준 0.4
- [ ] 새 영상 3편(prime_suspect·wild2·iconic)을 태블릿에 넣기 — 이제 목표가 안 밀린다
- [ ] 모아나는 `F:/per/mp3/moana_tablet.mp4`(1.06GB)로 교체해 넣기
- [ ] ⚔️ 배틀 하루 3번이 되면서 **한 포켓몬으로 하루에 3번 져서 잃을 수도** 있다 — 진우가 속상해하면 `lossesToLose`를 올리거나 하루 패배 상한
- [ ] 📊 부모용 추세 화면 (데이터는 이미 쌓여 있음)
- [ ] [나중] 🔢 수학 과목 — 태블릿 검증이 끝난 뒤에

## 2026-09-19~20 (v83~v91) — 실기기 신고 대응 + 🔤 새 게임 + 📁 가져오기 개선

### 진행 내용
아버님·진우가 태블릿에서 쓰며 준 신고를 받아 고치고, 요청한 기능 둘(🔤 단어 이어 주기, 🖤 까망 염색약)을 만들고, 📁 가져오기 흐름을 세 번 다듬었다. 중간에 `/codex` 건설적 리뷰를 한 번 받아 P2 5건을 전부 반영했다.

- **🔁 복습 빈도 (v83)**: "복습이 잘 안 나온다" → 확률 20→30%, 쿨다운 8→7문장. 2만 회 시뮬레이션으로 40문장/일 3.30→4.14회(+26%) 확인. **확률보다 쿨다운이 천장을 정한다**(쿨다운 8이면 산술 상한 4.4회라 확률만 50%로 올려도 4.36회에서 막힘)
- **🎟️ 교환권 (v84·v85)**: ① 팬텀 배달 뒤 `ash_battles`(이미 준 영상)가 다음 광고로 떠서 아버님이 넣을 게 없었다 → 목록에서 빼고 새 3편을 순서대로 ② **사자마자 다음 조건이 꽉 차 있던 것** → `profile.unlockBase`를 코인 차감과 같은 트랜잭션에 박고 거기서부터 셈 ③ 배달 끝난 영상은 목록에서 제거(제목 불일치로 "샀어요" 카드가 안 사라지던 문제)
- **🔤 단어 이어 주기 (v86)**: 왼쪽 영어 5 ↔ 오른쪽 한글 5를 탭 두 번으로 잇는 게임. 보상 [30,20,10]/[5,3,2]. 전환 경로 4곳에 배선
- **🕺 포켓몬 움직임 (v86)**: PokeAPI 5세대 애니메이션 GIF(명단 100마리 전부 확인). 기존 화면은 CSS로 잡기 후보 통통·쉬는 중 숨쉬기
- **🔤 신고 4건 + Codex 5건 (v87)**: "2~3장면마다 뜬다"(쿨다운 없음) · "경험치 얘기가 없다"(+`[object Object]`) · "선이 붙어 안 보인다" · "포켓몬이 안 나온다"
- **🖤 까망 염색약 (v88)**: 진우 요청. 실제 일러스트 넷으로 후보 다섯을 비교해 확정
- **📁 가져오기 (v89~v91)**: 세 번 고르던 것 → 한 번에 고르기 → 폴더 고르기 → **폴더에서 넣을 영상 탭으로 고르기**

### 변경 파일
- 신규 `js/match.js`, `js/sprite.js`, `js/importfiles.js`, `tests/{match,importfiles}.test.js`
- `js/{player,library,db,track,xp,items,unlock}.js`, `css/style.css`, `index.html`, `sw.js`(v82→v91)
- `tests/{player.logic,items,unlock,db.merge}.test.js` — 350개 → **405개**

### 결정사항 / 메모
- **"새 단어 20개마다"만으로는 간격이 안 잡힌다** — 단어장에 재고가 수백 개면 20개를 써도 남아서 매 문장 열린다. 쿨다운(20문장) + 하루 상한(3판)이 함께 필요했다. 📌 조건을 "쌓인 양"으로 걸 때는 **재고가 이미 많을 때**를 반드시 계산해 볼 것
- **메모리 사본 vs 저장소** — `state.vocabViews`는 콘텐츠 열 때 뜬 사본인데 단어 기록은 `track`이 IndexedDB에 직접 쓴다. 19개로 시작한 세션은 그날 내내 게임이 안 열렸다(Codex가 잡음). 판정 직전에 다시 읽는다
- **보상은 받은 자리에서 보여 준다** — 닫은 뒤 알림으로만 알리면 아이는 "없다"고 느낀다. 그 알림마저 `⚡+[object Object]`였다(`awardXp`는 객체, `awardCoins`는 숫자). 📌 **메시지에 값을 넣을 때 그 값의 타입을 확인할 것** — 테스트 378개도 헤드리스도 못 잡았다
- **"껐다 켜면 낫는다" 패턴을 네 번째로 또 만들었다** — `sprite.js`의 `missing` Set(한 번 실패하면 앱 재시작까지 포기). 메모까지 해 뒀는데 새 파일에서 반복했고 Codex가 잡았다. 새 모듈에 전역 Set/Map/boolean을 선언할 때마다 자문할 것
- **확인 없이 "브라우저에서 불가능하다"고 단언하지 말 것** — 폴더 고르기를 막아 버렸는데 **안드로이드 Chrome 147부터 지원**(태블릿 152)이었다. 아버님이 다시 물어서 드러났다. 다시 물으면 다시 확인한다
- **아버님은 영상을 한 폴더에 몰아서 보관한다** — 편마다 폴더가 나뉜 줄 알고 만들었다가 고쳤다. 그래서 폴더를 고르면 **넣을 영상을 목록에서 탭**으로 고른다(교환권으로 한 편씩 줘야 하므로 여러 편 동시 등록은 안 된다)
- **제목 자동 채움으로 배달 사고를 구조적으로 없앴다** — 파일명이 교환권 목록에 있으면 그 제목을 그대로 넣는다. 전날 "제목을 글자까지 같게 입력해 주세요"라고 당부했던 것이 필요 없어졌다
- **까망은 hue-rotate로 안 된다** — 색을 돌리는 것이지 어둡게 하는 게 아니다. `grayscale + brightness(0.4) contrast(1.5)`. 더 어두우면 누군지 모를 실루엣
- **`tools/check.mjs` APP_SHELL 가드가 두 번 더 일했다** (match.js · importfiles.js)
- **bash heredoc으로 문서를 쓰면 백틱 안이 셸에서 실행된다** — 체크리스트에서 두 번 당했다. 문서도 Write 도구로 쓰고 python으로 합칠 것

### TODO (다음 작업)
- [ ] **태블릿에서 확인**: 폴더 고르기가 뜨는지(Chrome 152라 될 것으로 보이나 미확인), 🔤 게임 빈도(쿨다운 20문장·하루 3판), 🖤 까망, 📁 새 가져오기 흐름
- [ ] 새 영상 3편(iconic·wild2·prime_suspect) + 모아나 1.06GB 넣기
- [ ] ★ **🔁 복습이 지금 연 영상의 문장만 낸다** — 새 영상을 연 날은 복습이 0회다. 교차 영상 복습은 다른 영상 소리를 못 쓰니 TTS(`essay.js:429` 재사용)가 필요
- [ ] ⚔️ 배틀 하루 3번이라 한 포켓몬으로 하루에 다 질 수도 있다 — 진우가 속상해하면 `lossesToLose` 조정
- [ ] 📊 부모용 추세 화면 (데이터는 이미 쌓여 있음)
- [ ] [나중] 🔢 수학 과목 — 태블릿 검증이 끝난 뒤에

## 2026-09-20 (v92~v96) — 아빠 교정문 · 📮 에세이 모아보기 · 🏠 과목 카드 · 🔢 수학 분수 줄기 1차

### 진행 내용
아버님이 보낸 에세이 사진(v92)으로 시작해 "언제꺼까지 고쳐줬는지 헷갈린다"(v93)를 고치고, 그다음 **수학을 두 번째 과목으로** 착수했다(v94~v96). 하루에 다섯 번 배포.

- **👨‍👩‍👦 아빠 교정문 3개 (v92)**: 진우 09-17 에세이 `[1][2][3]`을 `coach/fixes.json`에 적어 배포. 아이가 쓴 단어(space·boys·grade·math)는 살리고 문법만. `matchFixes` 앞 4단어 매칭으로 옮겨 적은 글이 조금 달라도 붙는 것 확인
- **📮 고쳐 줄 에세이 모아보기 (v93)**: 아버님 질문 "언제꺼까지 고쳐줬는지 알 수 있어?" → 안 고친 글을 맨 위에 모아(오래된 순 = 번호 순) 사진 한 장에 담기게, 고친 글은 접어 둠, 제목에 개수 + "마지막으로 고쳐 준 글: 날짜 (N일 전)". ★ **진짜 구멍은 7일 제한** — 안 고친 글이 8일 묵으면 화면에서 사라져 영영 못 고쳤다. 안 고친 글은 날짜 제한 없음
- **🏠 과목 고르기 (v94)**: 앱을 켜면 English(푸린) / Math(후딘) 카드. 🎒·📊·🚪는 과목 공통이라 홈으로, 영어 화면엔 `← 과목`. `js/home.js` `SUBJECTS`에 한 줄이면 과목이 는다. 영어 코드는 한 줄도 안 건드림
- **🔢 수학 설계 (아버님 원칙)**: ① 객관식만(펜 없음, 연습장에 풀고 답만) ② 개념 하나가 학습 단위 ③ "정말 이해했는지 검증하고 또 검증" ④ 초4~초6 + 중1은 꼭 필요한 개념 ⑤ 문제 속 세계는 포켓몬 70% + **끝까지 본 영어 영상** 30%, 사람은 진우 ⑥ 코드 생성(외우기 방지)과 사람이 쓴 내용(이야기)을 **섞는다** ⑦ 도감·코인 공유(그래야 "수학을 해서 마스터볼을 산다")
- **분수 줄기 10개념 생성기 (`js/mathgen.js`)**: 씨앗 난수(재현 가능), 한 개념을 세 얼굴로 — ①계산 ②오개념("{포켓몬}이 …라고 했어요. 진우야, 무엇이 틀렸을까?") ③왜 그런가. ★ **오답 = 흔한 오개념 + tag** → 틀린 답이 곧 진단. 📏 진단 5문제로 시작점(틀린 가장 앞 개념). 6만 문항 자동 검사(`tools/mathdump.mjs`)
- **사람이 쓴 내용 (`coach/math/fraction.json`)**: 📖 개념 이야기 10편 · ③ 50개 · ⭐ 특별 문제 54개(포켓몬 + 토이스토리5·미니언즈·모아나 — 2026년 영화라 `F:/per/mp3/*.en.srt` 자막을 읽고 씀). 검산 테스트가 식을 직접 풀어 정답 대조, `tools/check.mjs` 가드. 검수 페이지(`tools/mathreview.mjs`) → https://claude.ai/artifact/V381xEKwYKX3o5rz39FE5D (아버님이 출근 중 폰으로 검수, 항목 번호 `A6-⭐2`로 피드백)
- **그림 (`js/mathdraw.js`)**: 막대·피자·나란한 막대 SVG, `[bar 7/8]` 지시문. 분수의 뜻·같은 분모·가분수·통분에만(곱셈·나눗셈은 답을 흘림)
- **화면 (`js/math.js`, v95)**: 진단 → 사다리(👑 ▶ 🔒 + 🔁 오늘 복습) → 📖 → ①②③⭐ 4문항(즉시 피드백) → 결과. 보상은 `xp.js`·`catch.js` 그대로(처음 통과 ⚡40 💰12 🎯). 진도는 `profile` 스토어 `'math'` 레코드(스키마 불변, `updateMath` 한 트랜잭션 RMW, 백업 병합 `mergeMath`)
- **Codex 건설적 리뷰 10건 전부 반영 (v96)**: 같은 날 여섯 번 풀면 👑(연습은 `isDue`일 때만 라이트너) / 오답이 정답과 같은 값(`6/36`=`1/6`, 값으로 중복 제거) / "분모를 얼마로?"는 24도 답("가장 작은") / 곱셈에서 통분은 틀린 게 아님(오개념 문항을 실제 오류로) / 저장 중 이탈·백업 병합·저장 실패·잡은 포켓몬 1마리·📊 수학 칸

### 변경 파일
- 신규 `js/{home,math,mathgen,mathdraw,mathprog}.js`, `coach/math/fraction.json`, `tools/{mathdump,mathreview}.mjs`, `tests/{home,mathgen,mathcontent,mathworld,mathprog,mathreview}.test.js`
- `js/{app,db,stats,pokedex}.js`, `index.html`, `css/style.css`, `coach/fixes.json`, `tools/check.mjs`, `README.md`, `sw.js`(v91→v96)
- 테스트 405 → **461개**

### 결정사항 / 메모
- **수학은 학년이 아니라 줄기** — 진우가 5·6학년 선행 중이라 초4부터 시키면 떠난다. 줄기 A분수 B소수 C약수배수→소인수분해 D규칙→문자와식 E음수(중1 신규) F도형 G자료. **E·D가 중1에서 무너지는 자리**
- **코드 생성 vs 사람이 씀** — 아버님 "네가 만들어서 올리면 안 돼?" → 유한 문항의 함정(하루 15문항이면 열흘 안에 반복 → 답을 외워 "이해"와 "기억"을 구분 못 함)을 설명하고 섞기로. 계산·오개념은 코드, 이야기·왜·특별은 사람
- **문제마다 온라인 검색은 불가** (기기에서 오프라인 생성) — 소재는 만들 때 미리 틀로. 영상 세계는 앱이 진행률 90%↑인 것만 넘긴다(스포일러 방지, 테스트로 고정)
- **수학 문제의 정답성은 문자열이 아니라 값으로 검사** — 제 검산 테스트가 특별 문제만 값을 봤고 생성 문항은 글자만 봐서 `6/36`을 못 잡았다 (Codex가 잡음)
- **이미 아는 개념은 `isDue`일 때만 라이트너가 움직인다** — 판정을 저장소의 rec로 하면 두 창 경쟁도 저절로 풀린다
- **"화면을 지우고 await 하는 함수는 await 뒤에 한 번 더 비운다"** — v71 교훈을 그대로 또 밟았다("기록하는 중…" 잔상). `ui.run` 번호로 이탈도 막았다
- **번호 매기는 순서와 그리는 순서가 다르면 화면에서 뒤집힌다** (📮 [2][3][1]) — 순수 함수 테스트로는 못 잡는다, 헤드리스가 잡았다
- **bash heredoc은 `<<'PY'`로 인용해도 백슬래시를 한 겹 벗긴다** (이 PC의 Git Bash). `\\n`이 줄바꿈으로, `\b`가 **백스페이스 문자**로 파일에 박혔다(`cat -A`로만 보임). `python -c "…"` 큰따옴표 안 백틱도 셸이 실행한다. **코드·정규식·문서는 무조건 Write/Edit** — 이번 세션에만 네 번 당했다
- **/codex 컨설트 모드**: 이미 머지된 커밋 리뷰는 영어 프롬프트 5KB(파일 목록 + 설계 원칙)만 주고 Codex가 저장소를 읽게 하는 게 낫다 (57만 토큰·6분·10건 file:line). 인라인 56KB는 불필요

### TODO (다음 작업)
- [ ] **태블릿에서 v92~v96 확인** — 🏠 카드, 📮 에세이, 🔢 진단 5문제 → 첫 개념(보기 버튼 크기·세로 분수 가독성·이야기 길이), 📊 수학 칸
- [ ] 아버님 검수 페이지 피드백 반영 (`A6-⭐2` 번호로)
- [ ] 진우 문제집 스캔 사진 → 그 유형으로 생성기 틀·특별 문제
- [ ] 🛟 `backup.js` 사본에 `math` 레코드 포함 (진도는 다시 만들 수 없는 것)
- [ ] 다음 줄기: **E 음수 · D 문자와 식** (중1 고비) — 분수 반응 본 뒤. 착수 전 Codex 사전 리뷰
- [ ] (영어) 🔁 복습이 지금 연 영상 문장만 냄 → TTS 교차 영상 복습 / 새 영상 3편 + 모아나 1.06GB 넣기 / ⚔️ 하루 3번 패배 상한

---

## 2026-09-20 밤 ~ 2026-09-21 (v97~v103) — 진우가 수학을 처음 써 본 저녁의 신고 → 풀이 카드·쌍둥이·오답 노트

### 진행 내용
- **E 음수 줄기 1단계 (원고, 화면 없음)**: 아버님 "아직 배우지 못한 개념인데 문제부터 보여줘도 될까?" → 솔직히 "지금 구조(📖 한 편)로는 부족" → **"처음 배우는 줄기" 구조** 확정: 단계식 배움 4~5장(say → 확인 질문 → …) + 수직선 그림(`[line]` `[vline]` `[walk]`) + **아빠 카드**(저녁 10분: 말할 거리·활동·함정·통과 기준). 개념 9개 사다리(`js/mathneg.js`), 배움 39장·확인 34·아빠 카드 9(`coach/math/negative.json`), 검수 페이지 https://claude.ai/artifact/ENVs1zSA3AGUja9Pd16mcm — 아버님이 집에서 진우와 해 보고 막힌 자리를 번호로 알려 주기로(왕복)
- **실기기 신고 5건 (v97)**: 🎒·📊를 영어·수학 상단에도(`data-open`, 뒤로는 연 화면으로, 수학은 풀던 편 이어서) / **수학 일지**(편마다 문항별 정오·오개념, 개념별 얼굴별 누적, 📊 개념별 표 + **📋 수학 기록 복사** 버튼) / 자시안 거수참·자마젠타 거수탄(`battle.SIGNATURE`) + 자마젠타 명단 / "피자 얘기만 반복" → 생성기가 방금 나온 틀·문항을 피함(`opts.recent`, `tplKey`) + 틀렸을 때 📖 대신 🤔 되짚기 카드
- **아빠 교정문 9개 + 명단 60마리 (v98)**: 사진 두 장의 에세이 [1]~[9] → `coach/fixes.json`. 진우가 흔함·보통을 거의 다 잡아 60마리 추가 — `tools/roster_candidates.mjs`(PokeAPI 포획률로 등급: 흔함 42·보통 16·희귀 2·전설 0, 1~9세대, 10세대는 PokeAPI에 없음). 명단 101 → **161**
- **풀이 카드 + 쌍둥이 문제 (v99)**: 진우 "틀리면 한 문장만 나온다 — 그림·풀이로 왜 틀렸는지 어떻게 풀어야 했는지". ①② 생성기가 `solve{steps, why{tag}, figure, compare, rule}`를 **그 문제의 숫자로** 같이 만든다(개념 10개 × calc·misread 전부). 카드 = ❌내 답/✔정답 → 1️⃣ 왜 틀렸나(+내 답·정답 막대 비교) → 2️⃣ 이렇게 풀어요 → 3️⃣ 기억할 것 → **[비슷한 문제 하나 더]**(같은 틀·다른 숫자, 통과 여부 불변, ⚡1, 일지 fx). 되짚기 카드가 틀린 문제의 풀이를 접어서 보여 줌
- **Codex 건설적 리뷰 1차 13건 → 10건 반영 (v100)**: ★ "둘 다 뒤집으면 답이 거꾸로"는 **틀린 설명** / ÷자연수 ② "3을 3으로 나누려다 막혔다" 모순 / "공통 분모 = 최소공배수" 과잉 일반화 / 🎒 다녀오면 풀이·쌍둥이 건너뜀(채점·그리기 분리, `chosenIdx`) / 쌍둥이 같은 숫자 / 포커스 스크롤 / 자동 사본에 math
- **2차 (v101)**: ③ 50 + ⭐ 54에 사람이 쓴 풀이 → `fraction.json` `explain`(check.mjs가 형식·이름표·그림 검사). **🤔 오답 노트** `rec.notes` — 틀린 유형(key)이 남고 그 개념 다시 열면 한 문제 끼어 듦(`want`), 처음에 맞히면 지움. 검수 페이지(풀이 포함) https://claude.ai/artifact/59xPDPKLEedeCXS77ZnkHr
- **오답 노트 회차 (v102)**: 사다리 "🤔 틀렸던 유형 N개 다시 풀기" — 어제 이전 것만 최대 4, 개념 일정과 무관, 틀리면 내일 이후. 📊 "아직 못 고친 유형" chips
- **Codex 2차 리뷰(세션 이어서) 11건 → 반영 (v103)**: ★ **사람이 쓴 문항에 정답과 값이 같은 오답 2건**(A10-③3 "4/2"=2 · A7-③4) → `checkContent`가 값으로 검사 / ⭐ 이름표와 계산이 안 맞는 오답 2건 / 저장 실패 뒤 🎒 다녀오면 앱이 죽음 → "다시 저장" / 옛 백업이 고친 노트를 되살림 → `cleared` / 틀이 없어진 노트 억지 "고침" → 삭제 / `want`가 한 편 모든 얼굴에 퍼짐 → `{k, key}` / 노트 회차 fx·활동 보고 / again 보존 / 쌍둥이 숫자 서명

### 변경 파일
- 신규 `js/mathneg.js`, `coach/math/negative.json`, `tools/{mathlesson,roster_candidates}.mjs`, `tests/{mathneg,mathlog,mathsolve}.test.js`
- `js/{math,mathgen,mathprog,mathdraw,db,backup,stats,pokedex,pokemon,xp,battle}.js`, `index.html`, `css/style.css`, `coach/{fixes.json,math/fraction.json}`, `tools/{check,mathreview}.mjs`, `sw.js`(v96→v103)
- 테스트 461 → **499개**

### 결정사항 / 메모
- **"개념을 배운 뒤 문제"가 원칙** (아버님: "아이를 위해서 시간을 내면 되지 뭐") — 처음 배우는 줄기는 단계식 배움 + 아빠 카드. 학원 선생님 일 중 "처음 소개"만 아빠가, 반복·검증·복습은 앱이
- **쌍둥이를 맞혀도 그 편은 통과가 아니다** (아버님 확정) — ⚡1과 "바로 고침" 기록만. 오답 노트도 쌍둥이로 고친 건 남겨 두고 **다음 날 처음에 맞혀야** 지운다
- **풀이는 그 문제의 숫자로** — ①②는 코드가 만들고(무한), ③⭐는 사람이 쓴다(104개). 두 번의 Codex 리뷰가 **제가 쓴 설명의 수학 오류 3건과 문항 오류 4건**을 잡았다. "값이 아니라 글자로 검사한다"는 v96 교훈을 사람이 쓴 문항에는 안 걸어 뒀던 것 — 이제 `checkContent`가 ③⭐도 값으로 본다
- **`want`(틀 꽂기)는 얼굴별로만** — 한 편의 다른 문항까지 끌려가면 ③ 키가 계산 문항의 더하기/빼기를 정해 버린다
- 🎒·📊를 여러 화면에 둘 때는 `data-open` 한 곳 배선 + "연 화면으로 돌아가기" + **풀던 편 재개**(답한 뒤·쌍둥이 중·저장 실패 상태 모두)
- **node heredoc(`<<'JS'`)도 백슬래시를 벗긴다** — 이 세션에서 세 번(`\S`, `\[`, `\d`). 패치 스크립트는 **Write 도구로 `.mjs`에 쓰고 실행** — 후반부터 그렇게 해서 사고가 멈췄다
- Codex 세션은 같은 기능의 후속 리뷰면 **이어가기**(자기 지적이 고쳤는지 직접 확인) — 1차 10건 중 7 fixed·3 partial을 짚어 줬다. 138만 토큰
- 10세대 포켓몬은 PokeAPI에 없어(9세대 1025까지) 넣을 수 없다 — 그림도 거기서 받는다
- 📋 수학 기록 복사 → 아버님이 대화창에 붙이면 약한 개념을 보고 설명·문제를 만드는 흐름(요청 사항)

### TODO (다음 작업)
- [ ] **태블릿 실사용**: 풀이 카드 길이(폰 한 화면 반)·막대 비교 도움 여부·🤔 버튼이 다음 날 뜨는지·📋 복사가 카톡에 붙는지
- [ ] 음수 원고 검수 피드백(번호로, 집에서 진우와) → 원고 고침 → **E 음수 2단계 생성기**(처음부터 solve·값 검사) → 3단계 배움 화면(`storyBody` 한 줄 바꿈 처리) → 아빠 카드는 📊에
- [ ] 진우 문제집 스캔 사진 → 그 유형으로 생성기 틀·특별 문제
- [ ] 보류: 두 기기 병합 시각 충돌·kinds 최댓값(태블릿 한 대) / 13번째 노트 밀려남 / 충돌하는 오답 이름표 하나만 남는 것
- [ ] (영어) 🔁 복습이 지금 연 영상 문장만 냄 → TTS 교차 영상 복습 / 새 영상 3편 + 모아나 1.06GB 넣기 / ⚔️ 하루 3번 패배 상한

---

## 2026-09-21 낮~저녁 (v104) — E 음수 생성기·배움 화면 + "만지는 그림" + 감 잡기·틀린 이유

### 진행 내용
- **E 음수 2단계 생성기** (`js/mathneg.js`): 9개념 × ①계산·②오개념, 처음부터 풀이(solve) 포함. 부호 있는 `valueOf`로 보기를 **값**으로 겹침 제거(+5=5, −4/2=−2). 분수 생성기 부품(`castOf·worldPick·ask·solve·humanQuestion·checkHuman·ladderOf…`)을 `mathgen.js`에서 내보내 재사용 — 분수 출력 불변(Codex가 HEAD와 8,000개 비교). `tests/mathneggen.test.js`가 **식을 JS로 따로 계산해 정답과 대조**(생성기와 독립)
- **Codex 건설적 리뷰 10건 → 전부 반영**(새 세션, 78만 토큰): ★ ② "−6 + 7 − 7 = −6"처럼 **참인 등식을 틀렸다고 묻던 것**(b=cc) · −2²의 오답 "답은 −4예요"가 정답과 같은 값(문장 보기라 값 검사 못 잡음) · "음수가 더 커서" → 절댓값 · ② 갈래가 여럿인데 key 하나 → 쌍둥이·오답 노트가 다른 오류 유형으로 → `misread:갈래` · 이름표≠값("곱셈을 덧셈으로"인데 실제 더한 값이 아님) · "빚 2개인데 빚 9개 없애" → 장부 항목 지우기 · recent 회피가 가족 안에서 끝남 · 보충 '계산 실수' 49% → ≤1.4% · ③ 예비 18개에 풀이 · 조사. 테스트에 ②의 주장이 틀린 등식인지·문장 보기 값·이름표=값·보충 비율 검사 추가
- **아이디어 5개 제안 → 추천 순서대로 진행**: ① 만지는 그림 → ③ 감 잡기·틀린 이유 → ④ 오늘의 수학(섞어 복습) → ② 규칙 발견 표 → ⑤ 가르치기 모드
- **① 만지는 그림 + 음수 3단계 화면**: `mathprog.STEMS` 줄기 객체(분수·음수, 화면은 `S()/G()`만) · 🌳 줄기 고르기 · 📚 배움 화면(`lessonOf`, 한 장씩, ✋ 확인 질문은 고르기 또는 **🚶 끌어 보기** `check.walk:[출발, 이동]`, 맞혀야 다음 장, 📏 한 줄 → 문항, 🎒 복원) · `mathdraw.walkWidget`(끌기·눈금 탭·◀▶·잠금)·`shadeWidget`(분수 📖에 n/d 칠하기) · 풀이 카드에 내 답·정답 **두 점 수직선**(`solve.numline`) · 📊 수학 칸 줄기별 · 눈금 라벨 −(U+2212)
- **③ 🎯 감 잡기 + 🙈 틀린 이유**: 식 있는 문항은 보기 전에 부호(음수/0/양수) 또는 크기(1/2 미만·1/2~1·1 초과)를 고르고 나서 보기가 열림 — **답은 답한 뒤에 판정**(미리 알려 주면 보기 절반이 지워짐). 틀리면 실수/헷갈림/몰랐음을 골라야 다음 — **실수는 오개념·오답 노트에 안 쌓임**. 일지 `sn`·`w`, 📋 보고에 "감 잡기 n/m · 틀린 이유"
- 헤드리스(폰 폭) 전 경로 확인 후 **커밋 68383f8 · 배포 v104**(15초 반영, 배포본 cmp 일치, 라이브 콘솔 0). 테스트 499 → **524**

### 변경 파일
- 신규 `tests/{mathneggen,mathstem}.test.js`, `tools/mathnegdump.mjs`
- `js/{mathneg,mathgen,mathprog,mathdraw,math,stats}.js`, `css/style.css`, `coach/math/negative.json`(확인 질문 5개에 walk), `tools/mathlesson.mjs`, `tests/{mathneg,mathprog}.test.js`, `sw.js`(v103→v104)

### 결정사항 / 메모
- **아버님 회사라 검수 못 함 → "개발 먼저"** — 검수와 무관한 순수 로직(생성기)부터. 원고 검수·③⭐는 다음
- **②의 주장은 "틀린 등식"이어야 한다** — 숫자가 겹치면 참이 되는 주장이 있었다. 검산은 세 겹: 보기 값 겹침 / 식을 따로 계산 / ②의 주장·문장 보기·이름표=값
- **틀 이름표(tplKey)는 숫자만 지운다** — 조사(이라고/라고, 괄호 뒤 `)는`)와 부호는 남아 쌍둥이가 어긋난다 → 접히는 조사 5종만, 괄호·거듭제곱 뒤 조사 금지, 부호 자리 고정. ② 갈래가 여럿이면 key도 갈래별
- **구체 → 그림 → 기호**: 앱에 "만지기"가 없었다. 위젯은 정적 그림과 같은 자(`lineGeom`), 포인터 캡처는 svg에(퍼즐 v24 교훈), 눈금마다 투명 손잡이 26px
- **감 잡기의 답은 고른 직후엔 안 알려 준다** · **실수라고 한 말을 믿는다**(쌍둥이는 그대로 나와 거짓말 유인이 작다) · 진단·쌍둥이에서는 안 묻는다 · 이모지는 Emoji 12까지(🫣→🙈)
- Windows `/codex`: `python3`는 Store 스텁이라 파이프가 끊김 → `C:/Program Files/Python311/python` + `PYTHONUTF8=1` 파서로 한글 원문 그대로. 헤드리스 `$B js`는 async 반환값을 삼킴 → `window.__out`

### TODO (다음 작업)
- [ ] **태블릿(진우)**: 🚶 끌기 손맛(말 r=11px, 눈금 손잡이 26px)·눈금 탭 · 배움 장 길이 · 감 잡기·이유 고르기가 번거로운지 · ✋ 칠하기
- [ ] 음수 원고 검수(집에서, 번호로) — E4 "빚 2개인데 빚 6개 없애면 4개 이득" 표현을 원고도 장부 항목 지우기로 맞출지 → ③⭐를 `negative.json`에(코드 예비 18개가 나가는 중) → 아빠 카드는 📊에
- [ ] **④ 오늘의 수학** — 새로 배우기 1 + 다른 개념 섞어 복습 3 + 되짚기 1을 한 번 누르기로 → ② 규칙 발견 표(다음 칸 예측) → ⑤ 가르치기 모드(파트너에게 설명 녹음 → 아빠 확인으로 잠금 해제)
- [ ] Codex 리뷰(화면·위젯·감 잡기) — 태블릿 반응 뒤
- [ ] 진우 문제집 스캔 사진 → 그 유형으로 / (영어) 🔁 교차 영상 복습·새 영상 3편·⚔️ 패배 상한

---

## 2026-09-21 밤 ~ 2026-09-22 새벽 (v105~v108) — ☀️ 오늘의 수학 · ❓ 아빠에게 묻기 · Codex 두 번 · 🎬 beary_icy

### 진행 내용
- **☀️ 오늘의 수학 (v105)**: 사다리 맨 위 버튼 하나 → 개념 편 하나(🔁 복습 차례 우선, 없으면 ▶ 새 개념) → 🎲 배운 것 섞어 풀기(안 본 지 오래된 아는 개념 3개 × 약한 얼굴 1문항 + 🤔 어제 노트 1, 순서 섞음) → 완주 카드(하루 첫 완주 ⚡20 💰6). 아버님 결정 3건(섞어 풀기는 연습·복습 우선·20/6). `mathprog.dailyPlan/weakKinds/applyMixRound/markDaily`, 흐름은 `ui.daily`(🎒 다녀와도, 저장 중 이탈해도 이어짐)
- **Codex 3차 건설적 리뷰 9건 + 부가 → 8건 반영, #5 거절**(855K 토큰): ★ `schedAt`(일정 바뀐 시각) — `mergeMath`가 일정을 lastAt으로 골라 연습·섞어 풀기만 한 옛 기기가 👑을 되돌리던 **원래 있던 구멍**까지 막음 · 저장 중 🎒 이탈 시 흐름 소실 · 같은 틀 두 번 · '한 번 더'가 완주 횟수 올림 · **mix 문항 머리에서 개념 이름 제거(답한 뒤 "📚 통분 문제였어요")** · 90%↑ 얼굴은 튼튼함으로 묶어 순환. #5(두 번째 ☀️의 개념 편 보상)는 그 편이 번 것이라 거절
- **❓ 아빠에게 묻기 (v106, 아버님 제안 — 리뷰 중에 나온 아이디어)**: 아이는 😶 잘 몰랐어요·🤔 노트 또 틀림·편 결과 카드에서 ❓ → 문맥(개념·문항·식·내 답·정답·오개념·본 설명)은 앱이 담고 한마디만(선택) → 📮. 아빠는 📊 `❓ 질문함`에서 📋 복사 → Claude → 💬 붙여넣기 **또는** `coach/math/replies.json` 배포(둘 다, 멱등). 아이는 📬(읽어야 ☀️만 열림) → 😄 이해했어요 → 같은 틀 문제 → ⚡10 💰3 + 🤔 노트 지움 / 😶 → 한마디와 함께 다시 아빠에게. `js/mathask.js`(순수) + `m.asks`(math 레코드 안 → 백업·두 창·병합 한 번에), 홈 카드 📬 배지, `check.mjs` 가드
- **Codex 4차 리뷰 12건 전부 반영 → v107 핫픽스**(133만 토큰): 답장 멱등을 이력 전체로([A,B,A] 방지) · `understood→again` 허용(이해했다가 틀리고 답장 다시 보면 😶가 조용히 사라지던 것) · dup은 (개념, 틀) · 40개 정리는 끝난 것만 · `seenT`로 다른 창 새 답장 보호 · 병합 키 `t|본문` · 틀 불일치는 보상 분리 · cleared는 노트 없어도 · **제가 안내한 그림 문법 `[line -5 5]`가 틀림 → `[line -5..5]`, 가드가 실제 렌더러로 검사** · 전각 숫자 · 복사문에 보기·본 설명 · 문구
- **🎬 A Bear-y Icy Problem (v108)**: `F:/per/mp3/260922/beary_icy/` mp4·en.srt·ko.srt 29문장. 중복 5%(새 자료). 인식 오류 4건 재인식으로 확정(Forretress·Starmie·Beartic·Kyurem), PokeAPI가 또 잡음(쏘콘·툰베어·하나 지방). `dupcheck_srt.py` 신설, `align_srt.py` 버그 둘(~줄이 멀리 붙어 뒤가 밀림 / 끝 단어 보정이 앞 줄을 잡음) 수정, 단어장 +6
- 테스트 524 → **560**, 배포 4회 모두 배포본 cmp 일치·라이브 콘솔 0

### 변경 파일
- 신규 `js/mathask.js`, `tests/mathdaily.test.js`, `tests/mathask.test.js`, `coach/math/replies.json`, `tools/dupcheck_srt.py`
- `js/{mathprog,math,db,stats,home}.js`, `css/style.css`, `tools/{check.mjs,align_srt.py}`, `vocab/words.json`, `sw.js`(v104→v108)
- 메모리: `math-ask-roundtrip`(신규 — `❓N` 복사문이 오면 `💬N …`로만 답하기), `pokemon-video-pipeline`·`project-shincoach-state`·`project-shincoach-next-math`·`bash-heredoc-backslash` 갱신

### 결정사항 / 메모
- **섞어 풀기는 연습** — 1문항 정답으로 👑 확인 횟수를 올리면 "검증하고 또 검증"이 약해진다. 👑은 🔁 4문항 복습이 맡는다
- **섞어 풀기는 답하기 전엔 개념 이름을 숨긴다** — "어떤 개념인지 알아내기"가 인터리빙의 요점인데 제가 스스로 깎고 있었다(Codex)
- **❓ 답장은 아빠 이름으로** — 저는 초안, 아빠가 보내는 것(에세이 교정과 같은 원칙). 게이트는 ☀️만(수학 전체는 안 막음). 하루 3개, 같은 개념·틀은 하나
- **Codex 컨설트 요령**: 5~6KB 영어 프롬프트(파일·줄 번호, 아버님 결정은 재론 금지로 명시, 집 규칙, 질문 A~F) → 재현 스크립트까지 돌려 P1을 잡는다(7~12분). 새 기능마다 한 번은 돌릴 가치 — 제 실수(상태 전이 누락·문법 안내 오류)를 두 번 잡았다
- **`mergeMath`는 일정을 `schedAt`으로** — lastAt은 활동 시각이라 연습만 해도 오르고, 그걸로 일정을 고르면 두 기기 병합에서 👑이 되돌아간다
- 영상 파이프라인: `~`(버리는 줄)은 **whisper가 들은 대로** 적을 것(고친 이름으로 적으면 멀리 있는 진짜 단어에 붙어 뒤가 밀림) · 재인식이 매번 다른 짧은 줄은 울음소리 · PokeAPI 대조 필수
- heredoc 함정 세 번 더 — **긴 치환은 Write로 .py 파일을 만들어 실행**(fix_mathask.py·fix_screen.py 방식), 이후 사고 없음

### TODO (다음 작업)
- [ ] **태블릿(진우)**: ☀️ 개념 이름 없이 "🎲 섞어 풀기 2/4"가 막막하지 않은지 · ❓ 버튼이 눈에 띄는지·한마디 키보드가 번거로운지(말로 남기기는 다음 후보) · 📬 답장 길이 · 🚶 끌기 손맛·감 잡기
- [ ] 첫 ❓ 질문이 오면 `❓N` 복사문 → `💬N …` 답(초4·왜부터·그림 지시문 정확히) → 붙여넣기 또는 replies.json 배포
- [ ] ② 규칙 발견 표(다음 칸 예측) → ⑤ 가르치기 모드(파트너에게 설명 녹음 → 아빠 확인)
- [ ] 음수 원고 검수(E4 표현) → ③⭐를 `negative.json`에 → 아빠 카드는 📊에
- [ ] 지난 자료 prime_suspect를 고친 align_srt로 재생성하면 "What witness?" 한 줄이 살아남 — 원하시면
- [ ] 진우 문제집 스캔 → 그 유형으로 / (영어) 🔁 교차 영상 복습·새 영상 3편·⚔️ 패배 상한

---

## 2026-09-22 (v109~v118) — 🎮 수학 보상 경제: 수학 전용 포켓몬·🎯·✨·🎟️ 합산·🧤 스톤·🧭 레이더·🥚 알·🌈 이로치 + Codex 5~8차

### 진행 내용
- **출발점(아버님 관찰)**: 진우가 영어(들리는 대로 따라만)만 하고 머리 쓰는 수학은 잘 안 함. 코드로 본 원인 — 영어 10문장 ≈ ⚡100+🎯1(하루 4회)·교환권은 영어로만, 수학 ☀️ 8문항 ≈ ⚡70~85+🎯0 → 아이 선택이 앱 경제상 합리적. 제안 3축(순서 잠금·보상·부담)에서 아버님은 **보상 경제**만 고름
- **v109 🔢 수학 전용 포켓몬 150** — 기존 161은 그대로(영어), 새 150을 `subject:'math'`로(흔함 60·보통 45·희귀 30·전설 15, `tools/math_roster.mjs`가 PokeAPI 종 1..1025를 캐시해 포획률+진화 단계로 등급, 이름은 받은 것만 — "앤테이"). **도감·파트너·배틀·꾸미기·상점은 하나**, 갈리는 건 🎯 후보뿐(아버님 "모으는 곳이 갈리면 재미 반감"). 🎯 ☀️ 안 개념 편 통과 1 + 첫 완주 1, 🌟 섞어 전부 정답(하루 1, `markDaily(perfect)` 트랜잭션). 잡은 수학 포켓몬은 영어 퍼즐에 놀러 오되 잡히진 않음
- **v110 ✨ 오늘의 보너스** — 홈 수학 카드 반짝(빛줄기·✨, opacity/transform만) + 날짜 씨앗 보너스(🔵×2·⚡30·💰15·🌟·🟡) 첫 완주에 지급, 완주하면 "✅ 받았어요"
- **v111 🎟️ 교환권 합산** — `m.tot{ok,daily,rev}` + `unlock.MATH_PTS{2,20,5}`, 기준선에 수학 몫, 영어 카드 "📼 영어 n · 🔢 수학 m", 수학 쪽 "🎟️ 다음 영상까지 (이번 수학 📼+N · 🔁+M)"
- **v112 Codex 5차**(106만): ★ 번 몬스터볼이 사라짐(그림 0장·후보 받는 사이 🎒) → 레코드 pend + 사다리 "🎯 받은 몬스터볼 N개" · ★ 교환권 농사(3/4 틀리는 편 반복·두 번째 ☀️·❓ 이중) → 통과한 편만·첫 완주 섞어만·처음 고침만 · 🎒 위로 모달 · 8초 race
- **v113 🧤 스톤 1차** — 아버님 아이디어 "인피니티 스톤": **수학스톤·영어스톤**(가방 아이템, 못 삼, `purchaseRule cost.items` 그대로), "질"에서만(`stoneReward`: 개념 통과 1·👑 +2·노트 전부 고침 1·❓ 고침 1 / 복습 회차 완주·에세이), 🎒 건틀릿 줄(🔒 미래 과목), 🛒 스톤 상점, 🧭 레이더(💰100+🔷1)
- **v114 Codex 6차**(100만): ★ pend max 병합이 쓴 던지기를 되살림 → `throws{earned,used}` · 노트 재제출 이중 → `resolved` · `consumeItem` 메모리 판정 → `useItem` 트랜잭션 · 레이더 자동 → **아이가 눌러 쓰고 🧭 배지** · 영어스톤 전부 통과+하루 2
- **v115 🥚 알** — `js/egg.js` 순수(과목당 하나, 종은 살 때 `pickHatch`로 확정, 날짜 합집합, 5일이면 같은 트랜잭션에서 부화+caught++), `hatch.js` 🐣 화면, 하루치 = 수학 ☀️ 첫 완주 / 영어 목표 달성
- **v116 Codex 7차**(114만): ★ `resolved`를 만들고 **호출부 배선을 안 함** → 옛 폴백이 살아 있었음 · ★ 되돌리기(used−1)를 max가 지움 → `refunded` · ★ 알 "최근 쪽 통째로" 병합이 부화를 되돌려 재부화(재현) → 같은 알 합집합·묘비 · 회차 열쇠 `claimDailyKey` · tickEgg 메모리 검사 제거 · 부화 "봤다"는 OK 때
- **v117 🌈 이로치의 스톤** — 💰500+🔷3🔶3, `db.shinyRule` 한 트랜잭션, `mons[id].shiny` 영구(병합 OR), PokeAPI 이로치 일러스트를 characters 스토어 문자열 키 `"25:shiny"`로, `setFigure`가 `look.shinyUrl` 우선(변신 > 이로치 > 일반)·염색 끔·✨ 배지 → 도감·잡기·퍼즐·배틀·파트너·홈·부화 전부
- **v118 Codex 8차**(139만): "오늘 밤 아이가 부딪힐 3가지" — 이로치 그림 재시도 없음 → 팝업마다 재시도 · 시작 때 그림 캐시가 홈 뒤·다른 창·🔤 GIF → 홈 전에 await·visibilitychange 새로·이로치 정지 그림 · 이로치에 염색약 소모 → 거절+안내. 잠긴 레벨 포켓몬을 알로 얻으면 ???로만 → 잡은 칸에
- 테스트 560 → **582**, 배포 10회 전부 배포본 일치·라이브 콘솔 0. 헤드리스로 전 경로(오프라인 흉내·그림 0장·두 번째 ☀️·5일 부화·이로치)

### 변경 파일
- 신규 `js/{mathbonus,egg,hatch}.js`, `tools/math_roster.mjs`, `tests/{mathbonus,egg}.test.js`
- `js/{pokemon,xp,battle,player,math,mathprog,db,catch,pokedex,shop,home,unlock,library,track,items,app}.js`, `css/style.css`, `index.html`, `sw.js`(v108→v118), 테스트 8개 갱신

### 결정사항 / 메모
- **잠금 대신 보상** — 순서 잠금(프리맥)은 예비. 반응이 없으면 그때 "☀️ 먼저 → 영어" 검토
- **집 규칙이 실제로 버그를 막았다**: 하루 한 번은 트랜잭션 안 · 병합은 단조·입력 불변 · 보상은 화면과 무관, 화면은 보일 때만 · 소유와 그림 분리. Codex가 잡은 건 대부분 이 규칙을 "새 필드에 적용 안 한" 곳(pend 잔액, 알 목록, consumeItem, notes 배선)
- **Codex resume**로 4번 이어 돌리며 "네 지적이 고쳐졌는지"부터 검증시키는 구조가 배선 누락을 잡았다. 회당 100~140만 토큰·8~12분, 프롬프트 5~8KB(아버님 결정은 settled로)
- 라이브 확인: Pages CDN max-age 600 — 헤드리스는 `$B restart` 뒤에. 배포본 비교는 `--strip-trailing-cr`
- 보류: 던지기 예약 id(분기 이력) · 알 틱 실패 자동 복구(내구 완주 이벤트 없음) · 수학 기록↔프로필 보상 한 트랜잭션(영어도 같은 구조) · 영어 알 진행을 목표 옆에

### TODO (다음 작업)
- [ ] **태블릿(진우)**: 수학을 여는지·🔢 잡기·✨ 카드·🧤 스톤·🥚 알에 반응하는지 (📊 하루 mathQ vs 문장 수로 비율 확인)
- [ ] 미룬 아이템: 🧬 진화의 스톤(명단 안 진화 쌍) · 🏟 체육관 도전권 · 🧤 건틀릿 완성 보상(누적 10개씩)
- [ ] 반응 없으면 순서 잠금(☀️ 먼저 → 영어) 검토 · 첫 ❓ 질문 답장 · ② 규칙 발견 표 → ⑤ 가르치기


---

## 2026-09-23 ~ 09-24 (v119~v126) — 🔁 오늘의 복습 · 🚫 미니언즈 제외 · ⚔️ 수학 배틀 · ✨ 응원 포켓몬 · 🔢 B 혼합계산 줄기 + Codex 9차

### 진행 내용
- **🎬 아르세우스 연대기 학습자료** (`F:\per\mp3\260923\arceus\`) — 9분 1초·67문장. 중복 검사 통과(최대 2.2%). 재인식이 `Guntank/Bladethrower`→**Skuntank/Flamethrower**, `Prickly`→**Purugly**, `Thunder Balls`→**Thunderbolt**, `Azalf/Yuxi`→**Azelf/Uxie**, `it's hellion`→**Inteleon**, `It's hilarious`→**Cinderace** 를 바로잡음. PokeAPI 대조(히드런·아그놈·엠라이트·10만볼트·매지컬샤인·일렉트릭네트). 한글은 영어 타임코드 복사 → 싱크 0.00초. 단어장 +15 (v120)
- **🔁 오늘의 복습 (v119)** — 진우 "복습이 자주 안 나와요". 원인은 확률이 아니라 **구조**: 복습 후보가 `getSentenceStats(itemId)`라 **지금 연 영상의 문장만**이고, 새 영상을 열면 그날 0회·밀린 문장은 옛 영상에 갇힌다(시뮬레이션: 매일 다른 영상을 열면 30일 내내 0회). → `dueByItem/pickDueItem`으로 앱이 밀린 영상을 골라 열고 바로 시작(목록 버튼 + 홈 칩). 회차 3→5문항 + **한 번만 올리는 마이그레이션**(⚙는 기기별 localStorage라 기본값만 바꾸면 태블릿에 적용이 안 된다)
- **🚫 미니언즈 제외 (v121)** — 아침에 아이가 써 보고 "미니언즈 복습이 천 몇 개". 듣기만 해도 큐에 들어가는데 자막 대부분이 미니언즈어·비명이라 🔁 버튼이 늘 그걸 열었다. `reviewable(item)` = 제목 기본값(`/미니언/`) + 📊 콘텐츠별 스위치(부모 선택 우선). 아이 화면 숫자는 밀린 전체가 아니라 **지금 풀 만큼**만
- **🐼 쿵푸팬더 포기** — 아버님: 하드섭 추출이 PC를 몇 시간 먹통으로 만들고 재부팅까지 시켰다. 원인은 큐 1,503개마다 ffmpeg 프로세스를 새로 띄운 것(`WinError 299`). 도구에 경고를 박고 체크리스트에 사유 기록, `kfp4.mp4`(811MB) 삭제(원본 mkv는 남김)
- **⚔️ 수학 배틀 (v122)** — 진우 "수학에선 배틀이 안 나와요"가 맞았다(`maybeBattle`이 player.js에만). `openBattle({quiz})`면 문제 풀기 턴(`quizTier`: 정답 🎯·연속 2번째부터 🌟·**시간 제한 없음**). 확률·상한·보상은 영어와 같은 상수, 하루 3번은 `claimDailyCount('battles')`로 두 과목 공유. 배틀 문항은 기록에 안 남김
- **✨ 응원 포켓몬 (v123)** — 아버님 아이디어. 수학 푸는 동안 잡은 포켓몬이 화면 아래를 걸어가며 말풍선(한글+영어). 틀린 직후 50%·평소 18%·쿨다운 3·세션당 8. `pointer-events:none`으로 조작을 막지 않고, 수학 밖으로 나가면 지움. **수학 전용**(영어에 새지 않게 테스트로 고정)
- **🔢 B 혼합계산 줄기 (v123 원고 → v124 생성기)** — 진우가 직접 요청. 초5 자연수 → 초6 분수·소수, 사다리 8칸(addsub→muldiv→order→paren→brace→four→frac→dec). 아버님이 **원고 검수를 건너뛰고 2단계로** 결정. `js/mathmix.js`(mathneg와 같은 인터페이스) + `coach/math/mixed.json`(배움 35장·확인 29·아빠 카드 8) + `tools/mathmixed.mjs` 검수 페이지
- **Codex 9차 (v125·v126)** — 230만 토큰, 개념당 10,000 seed 직접 재현. 10건 전부 반영

### 변경 파일
- 신규 `js/{cheer,mathmix}.js`, `coach/math/mixed.json`, `tools/mathmixed.mjs`, `tests/{cheer,mathmix}.test.js`
- `js/{review,library,home,player,stats,math,mathprog,mathgen,mathneg,battle,track,db,app}.js`, `index.html`, `css/style.css`, `tools/check.mjs`, `sw.js`(v118→v126), `vocab/words.json`
- 메모리: `test-seed-breadth`(신규) · `heavy-local-jobs-crashed-pc`(신규→갱신) · `project-shincoach-state`(v126)

### 결정사항 / 메모
- **복습이 안 나오는 건 확률 문제가 아니었다** — 후보 풀이 "지금 연 영상"에 갇혀 있었다. 포켓몬 영상을 넣어 줄수록 복습이 마르는 구조였다. 신고를 받으면 **확률부터 만지지 말고 후보가 어디서 오는지**를 먼저 볼 것
- **⚙ 설정은 기기별 localStorage** — 기본값만 바꾸면 이미 저장된 태블릿엔 적용이 안 된다. 아버님이 낮에 출근하시므로 **배포로 적용되는 한 번만 마이그레이션**이 필요하다
- **오개념 이름표는 그 실수의 실제 값과 대조해야** — 덧뺄·곱나눗 칸의 오답에 "왼쪽부터"를 붙였는데 그 칸은 왼쪽부터가 **정답**이었다(실제 실수는 "뒤부터"). 이름표가 틀리면 📊 진단이 거짓이 된다
- **② 오개념 문항은 "등식이 거짓인가"만으론 부족** — 같은 값을 설명하는 보기가 둘이면 정답이 둘이다. **중간 줄**(`28 − 17 =`)을 보여 실수를 특정
- **테스트 seed 폭이 곧 잡는 힘** — 200개로 통과한 것을 Codex가 10,000개에서 깼다(음수 정답). 지금은 4,000 + "나오면 안 되는 값" 검사 → [[test-seed-breadth]]
- **스톤 전용 아이템의 원칙(제안)**: 스톤은 질에서만 나오므로 **학습을 쉽게 해 주는 것(힌트권·건너뛰기)은 안 된다**. 추천 셋 — 🧬 진화의 스톤(🔷3/🔶3, 명단 안 진화 쌍 **66개** PokeAPI로 실측) · 🏅 과목 배지·칭호(🔷5/🔶5, 코인으로 못 사는 유일한 꾸미기) · 🏟 체육관 도전권(🔷2🔶2). 🧤 건틀릿 누적 보상과 겹치므로 **사는 쪽**으로 합치기를 권함
- 헤드리스를 한 세션에 십수 번 띄우면 DevTools 연결이 실패하기 시작한다 — 끝날 때마다 프로세스·프로필 정리

### TODO (다음 작업)
- [ ] **스톤 전용 아이템** — 아버님이 고르면 착수 (추천: 🧬 진화의 스톤부터)
- [ ] **태블릿(진우)**: 🔁 복습 버튼을 쓰는지 · ⚔️ 수학 배틀 · ✨ 응원 빈도 · 🔢 혼합계산 줄기를 여는지
- [ ] 혼합계산 ③ why · ⭐ special 사람이 쓴 문항 (지금은 ①② 만) · 원고 검수 피드백 오면 반영
- [ ] 첫 ❓ 질문 답장 · ② 규칙 발견 표 → ⑤ 가르치기 · 음수 원고 검수(E4)
