# 신코치 (shincoach) 개발 진행 기록

> 새 세션 시작 시 이 파일과 `.task-memory/` 3종을 먼저 읽는다.

## 2026-09-12

### 진행 내용
- 요구사항 논의 → 로컬 mp4+srt/smi, 순수 HTML/JS PWA, IndexedDB 저장, GitHub Pages 배포로 결정
- 1~6단계 초안 구현: 골격, srt 파서, 가져오기/라이브러리, 플레이어(문장 이동·반복·속도·이중 자막·섀도잉·단어 하이라이트·이어보기)
- /codex 1차 리뷰 14건 중 P1 5건 + 파서 3건 반영 (백그라운드 일시정지, 탐색 동기화, ended 처리, 재생 오류 안내, SW 캐시 범위, 공백 구분줄/VTT ID, 한글 중복, 겹침 병합)
- SMI(SAMI) 자막 지원 (영·한 한 파일 자동 분리, CP949)
- 실제 영화 준비: ffmpeg 설치, mkv 내장 영어 자막 추출 3편, 720p mp4 변환(토이스토리5·미니언즈·모아나), 싱크 검증 도구
- GitHub Pages 배포(v1~v8), Tailscale 파일 서버로 집 노트북에 전송, 태블릿 설치
- 구형 안드로이드(삼성 인터넷 → Chrome 전환) 호환: 문법 하향, FileReader, 오류 배너
- 듣기 먼저 모드(영어 숨기고 N번 듣고 공개), 단어 패널(내장 사전 645→1,436단어 + 280표현), 반복 기본 ∞·설정 유지·섀도잉 순환

### 변경 파일
- `index.html`, `css/style.css`, `js/{app,db,srt,sami,library,player,vocab}.js`, `sw.js`, `manifest.webmanifest`, `vocab/*.json`, `tools/{synccheck.mjs,fileserver.py,vocab_extract.mjs,vocab_scan.mjs}`, `tests/*`

### 결정사항 / 메모
- 사무실 PC의 TRADEMY API가 상시 실행 → ffmpeg 등 무거운 작업은 Idle 우선순위 (CPU 100%로 폰 앱 타임아웃 사고)
- 미니언즈 영어 자막은 CC라 효과음 제거 로직 추가; 한글 smi의 두 화자 동일 대사는 한 줄로

## 2026-09-13

### 진행 내용
- ⚙ 설정 비밀번호(해시 보관), 듣기 먼저 4번 옵션, 기기 진단(마이크·음성 인식 테스트, 녹음 들어보기)
- 🎤 말하기 확인: 문장 끝 → 마이크 녹음 → Web Speech 단어 일치율(폴백: 말소리 길이) → 통과 전 다음 문장 차단, 3번 미달 시 통과, 마이크 불가 시 확인 없이 진행
- 📊 학습 기록 대시보드(부모, 비밀번호): DB v2(sentenceStats/sessions/daily/vocabViews), 주간 요약, 콘텐츠 진행률, 어려운 문장 TOP10(탭→이동), 복습 단어장, 세션, JSON 백업/복원. 아이 화면엔 ⭐정복·오늘의 목표 칩
- /codex 2차 리뷰 10건 전부 반영 (자동 이동 상태 초기화, 말하기 확인 수명주기, DB 업그레이드 차단 안내, 마이크 해제, 소음 바닥 최솟값, 백업 병합, 저장 재시도, 날짜 전환, 공개 시 완료 기록, 긴 문장 내용어 기준)
- v12 배포에 heredoc 백슬래시 깨짐 문법 오류 → v13 즉시 복구, 커밋 전 검사 스크립트(`tools/check.mjs`, `npm test`에 포함) 도입
- `.task-memory/`를 공개 저장소에서 제외 (비밀번호 메모 포함)
- 배포 v9 → v16

### 변경 파일
- `js/{speak,track,stats,diag}.js` 신규, `js/{player,db,app}.js`, `index.html`, `css/style.css`, `sw.js`(v16), `tools/check.mjs`, `tests/{speak,track,stats,db.merge,player.logic,vocab}.test.js` (총 74개)

### 결정사항 / 메모
- 태블릿 = 갤럭시탭 A7(Android 10) + Chrome 152. 처음엔 삼성 인터넷으로 열려 음성 인식 불가 → Chrome으로 재설치. 마이크 정상이지만 음량 작음(RMS 최대 0.03) → 말소리 문턱 상한 0.022
- 말하기 판정: 40% 일치 또는 (2단어+내용어, 6단어 이하) 또는 (3단어+내용어). 3번 미달 시 통과 (사용자 결정)
- 학습 기록 "완료" = 영어 공개 상태로 끝까지 들음(듣기 먼저면 공개 시점). ⭐ = 발음 80%↑. 📊는 부모만(비밀번호)
- Bash heredoc은 백슬래시를 깨뜨림 → 코드는 Write/Edit로, 푸시는 `npm test &&` 체인으로만

### TODO (다음 작업)
- [ ] [백로그] 태블릿 Web Speech 인식 결과 없음 원인 확정 — ⚙ 진단 "단계: start → …" 줄 확인
- [ ] 며칠 실사용 후 📊 기록 보고 판정 기준·표시 조정 (말하기 통과율, 어려운 문장 기준)
- [ ] 태블릿에서 v16 실사용 피드백 (말하기 확인 흐름, 오늘의 목표, ⭐)
- [ ] [보류] 아이 발음 녹음 보관·비교 듣기 (제안 4)
- [ ] 새 영화 추가 시 `tools/vocab_extract.mjs`로 사전 확장, README 절차 참고

## 2026-09-14

### 진행 내용
- 🧩 문장 퍼즐 게임 (v18~v20): 10문장마다 배운 문장의 단어를 섞어 드래그/탭으로 어순 맞추기. 단어 자리 고정(밀림 방지), ⚙ "지금 퍼즐 해보기", 정답 뒤 문장 한 번 더 듣기, 기록(puzzles/puzzleSolved) + 📊 KPI
- 따라 말하기 영어 숨김 3단계 (v19): 처음 숨김 → 못 하면 홀수 번째 단어 빈칸 → 그래도 못 하면 전부
- 🎮 포켓몬 캐릭터 (v21): `js/pokemon.js` 30마리 명단, ⚙ "받기"로 PokeAPI 공식 일러스트 → 256px → IndexedDB(characters, DB v3). 퍼즐 자리 = 말풍선+캐릭터 카드, 캐릭터 눌러도 단어 오감
- ⚡ 경험치·레벨·🎯 몬스터볼 잡기·🎒 도감 (v22): `js/xp.js`(규칙+프로필, DB v4 profile, 백업 포함), `js/catch.js`(볼 던지기 CSS 연출, 운 판정), `js/pokedex.js`(도감: 실루엣/???). 퍼즐 정답 30/20/10 XP, 문장 완료 +2, 말하기 통과 +3(⭐+5), 레벨 Lv1→2 100 이후 +40. 희귀도 4단계 기본 45/30/15/3% × (1+레벨×5%), 최대 90%. 정답마다 던지기 1회, 또 잡으면 +10
- 개발 편의: `tools/devserver.py`(no-store), `?nosw=1`(SW 등록 안 함)

### 변경 파일
- 신규 `js/{puzzle,pokemon,xp,catch,pokedex}.js`, `tests/{puzzle,pokemon,xp}.test.js`, `tools/devserver.py`
- `js/{player,db,track,stats,app}.js`, `index.html`, `css/style.css`, `sw.js`(v22)

### 결정사항 / 메모
- 퍼즐은 문장 전환 시점에 출제(말하기 확인과 안 겹치게), 단어 조각은 구두점·대문자 그대로(힌트)
- 포켓몬 그림은 저작권 때문에 저장소에 두지 않고 기기에서만 보관
- 헤드리스 테스트: localhost SW가 옛 파일을 줌 → devserver + ?nosw + 컨텍스트 재생성(viewport --scale 2→1)
- Write/Edit 도구는 유니코드 이스케이프를 실제 문자로 바꿔 저장 (정규식 동작 동일)

### TODO (다음 작업)
- [ ] 태블릿: v22 새로고침 → ⚙ 캐릭터 받기 → 퍼즐·잡기·도감 실사용 확인
- [ ] 아이 반응 보고 확률/XP 조절 (⚙에서 조절 가능하게 할지), 명단 확장
- [ ] 📊 대시보드에 레벨/퍼즐 그래프

## 2026-09-14 (이어서 — 태블릿 피드백·동기 시스템·리뷰 반영, v23~v28)

### 진행 내용
- 퍼즐 조작 피드백 (v23·v24): 정답 칸에서 단어를 빼면 **빈 자리(점선)** 가 남고 다음 단어가 거기로 들어감 / 드래그가 중간에 끊기던 원인 = 끌고 있는 조각을 DOM에서 실시간 이동 → 포인터 캡처 상실. 조각은 제자리(ghost)+복제본만 이동+자리표시(drop) 미리보기, 이벤트는 document 리스너로 재작성
- 🔊 효과음(Web Audio 합성)·📳 진동·🎊 종이가루·두근두근 무대 흔들림, ⚙ 효과음/진동 토글 (v25)
- 동기 시스템 3종 (v26): 🔥 연속 학습일 스트릭(하루 5문장, 보너스 10~40 XP) / 🔒 레벨 마일스톤(명단 60마리, Lv5·10·15에 10마리씩 해금, 도감 잠금 표시) / 🎯 오늘 목표 달성 +30
- /codex 건설적 리뷰 → P1 1·P2 6·P3 제안. 6건+2건 반영 (v27): 프로필 **증분 저장**(두 창 동시 실행 안전, `applyProfileDelta`), 화면 꺼진 동안 잡기·퍼즐 자동 종료 보류, 마지막 문장 퍼즐, 자정 스트릭 재계산, 목표 보너스 하루 1회(`goalRewarded`), XP 지급 시 기록 즉시 flush, 오늘 첫 퍼즐 5문장, 다음 해금까지 남은 XP 표시
- 부족한 캐릭터 자동 받기 (v28): 앱 시작·online 이벤트 때 명단보다 적으면 조용히 받아옴
- gstack 1.79 → 1.84.1 업그레이드

### 변경 파일
- 신규 `js/sfx.js`, `js/{puzzle,catch,xp,pokemon,pokedex,player,db,track}.js`, `index.html`, `css/style.css`, `sw.js`(v28), `tests/*` (총 111개)

### 결정사항 / 메모
- 드래그 중 캡처된 요소를 `insertBefore`로 옮기면 Chrome이 캡처를 놓침 → 절대 옮기지 말 것 (자리표시만 이동)
- 프로필(XP·잡은 포켓몬)은 통째로 put하지 않고 증분을 한 트랜잭션에서 더함 — 홈 화면 앱+Chrome 탭 동시 실행 대비
- 오버레이 자동 종료는 `document.hidden`이면 보류(`pendingAuto`) → 잠긴 태블릿에서 소리 안 남
- Codex 실행: Windows에서 큰 프롬프트는 `codex exec -` + stdin (인자 길이 제한 exit 126)
- 명단을 늘릴 때 `RARITY_IDS`에도 등급 추가 (테스트가 전부 등급 있는지 검사)

### TODO (다음 작업)
- [ ] 태블릿 실사용: v28 → 드래그 매끄러운지, 잡기(소리·진동·종이가루), 도감, 자동 받기 60마리
- [ ] Codex #7: 가로 모드·큰 글자에서 카드가 스크롤되면 드래그로 정답 칸까지 못 감 → 가장자리 자동 스크롤 (실기기 확인 후)
- [ ] 아이 반응 보고: 전설 실패 누적 보상, 퍼즐 XP 비중, 첫 시도 콤보, 📊에 레벨/퍼즐 그래프

## 2026-09-14 (이어서 — 아이템 1·2단계·부모 모드, v29~v31)

### 진행 내용
- 🛒 아이템 1단계 (v29): 💰 코인(학습 행동으로만 획득: 문장 1·말하기 2/⭐3·퍼즐 5/3/2·목표 10·스트릭 5×일·레벨업 🎁 상자) + 🛒 상점(🎀 장식 10종 개수 보유, 🎨 염색약 7종 소모품·원래 색 무료) + 도감에서 포켓몬 탭 → 장착/염색 모달 → 퍼즐 카드·잡기·도감에 반영(이모지 오버레이 + CSS filter)
- 👨‍👩‍👦 부모 모드 (v30): ⚙(비밀번호) 토글. 반복·듣기 먼저·섀도잉·말하기 확인·퍼즐 없이 연속 재생, 기록·XP·코인 안 쌓임, 상단 칩으로 비밀번호 없이 복귀, 앱 재시작 시 해제. 망나뇽을 전설→희귀로 (아이 지적)
- 🛒 아이템 2단계 (v31): 🤝 파트너(자동 지정, 도감에서 변경) + ❤️ HP 100 — "틀림"이 아니라 "대충 넘김·안 함"(정답 공개 −20·말하기 3번 미달 −10·어제 안 함 −30)에만 깎이고 0이면 😴 쉬는 중(잃지 않음) + 🧪 물약 10/25코인 + 목표 달성 +20 + ⚙ HP 켜기/끄기

### 변경 파일
- 신규 `js/items.js`(코인·카탈로그·HP 규칙·makeFigure), `js/shop.js`(상점·상세 모달), `tests/items.test.js`
- `js/{xp,db,track,player,puzzle,catch,pokedex}.js`, `index.html`, `css/style.css`, `sw.js`(v29→v31), `tests/player.logic.test.js` (총 126개)

### 결정사항 / 메모
- 코인은 XP와 별개 화폐(XP는 레벨·해금). 프로필 백업 병합에서 coins·items·mons·partner는 최근 저장 쪽 통째로(max면 옛 백업이 코인을 되살림)
- 이모지는 Android 10 = Emoji 12까지 → 🪙·🩷 대신 💰·💗. ZWJ 조합도 피함
- 염색 필터는 실제 일러스트로 비교해 확정. 밝은 노랑(피카츄)은 빨강이 연함 — 한계
- 파트너가 없는 기존 프로필은 가장 많이 잡은 포켓몬을 자동 파트너로(ensurePartner)

## 2026-09-15 (말하기 인식 우선·여행 길·배틀, v32~v36)

### 진행 내용
- 태블릿 "대충 말해도 통과" 원인 확정: 안드로이드 Chrome은 getUserMedia 스트림이 열려 있으면 음성 인식이 소리를 못 받음(진단 "동시 사용 테스트"로 재현). 그동안 소리 길이만으로 판정됐음
- v32: 인식 실패 이유를 결과 화면에 표시(🎙 인식 안 됨(이유)) + ⚙ 진단 동시 사용 테스트 / v33: speak.js를 **인식 우선**(releaseMic 후 인식만, 말소리는 인식 이벤트) + 안 되면 에너지 폴백으로 재구성 → 태블릿에서 단어 표시 확인됨
- 🗺️ 여행 길 (v34): 진행 바 → 🏠→🏁 길, 풍경 6~10개(콘텐츠 id 시드), 파트너 포켓몬이 문장 위치에서 걸어감, 도착 시 🎊 + ⚡50 💰20(콘텐츠당 1회)
- ⚔️ 트레이너 배틀 (v35): 문장 완료 시 3%(오늘 5문장↑, 하루 1번) → 다음 전환에서 등장(수락/거절). 공격 = 문장 듣고 따라 말하기 → 등급별 데미지(강한 40/25/8/0, 확실한 28/22/15/5), 상대 15~25, 물약 턴. 승 = 상대 포켓몬 획득 + ⚡40 💰15, 패 = 패배 +1, **3번 누적 시 1마리 잃음**(사용자 결정), 파트너 제외. ⚙ 배틀 연습
- /codex 건설적 리뷰(8건) 중 6건 반영 (v36): 결과 화면에서 닫아도 승패 보존 / 화면 꺼짐 시 턴 무효 / 마지막 문장 섀도잉 뒤 배틀 / 뒤 화면 inert + 연습 진입 가드 / 듣기 건너뛰기·다 말했어요 라벨 + 소리 피드백 / 테스트 보강
- gstack 1.84.1 → 1.87.0

### 변경 파일
- 신규 `js/battle.js`(규칙·타입 60마리·UI), `tests/battle.test.js`, `tests/speak.flow.test.js`
- `js/{speak,player,diag,xp,db,track,items,catch,pokedex}.js`, `index.html`, `css/style.css`, `sw.js`(v32→v36), `tests/player.logic.test.js` (총 143개)

### 결정사항 / 메모
- 새 마이크 기능은 인식과 동시에 getUserMedia를 열지 말 것 (안드로이드 제약)
- 배틀의 공격을 학습(따라 말하기)으로 묶은 이유: 운·상성만이면 학습과 분리돼 "이것만 하려는" 위험. 여행 길·배틀·HP 모두 파트너 1마리에 집중
- 배틀 턴의 말하기는 퍼즐의 🔊 재생 메커니즘(state.puzzleCue·playPuzzleSentence) 재사용, 배틀 뒤 continue는 force:true로 같은 전환에서 퍼즐 중복 방지
- Codex 한글 응답이 Windows 파서(CP949)에서 깨짐 → ~/.codex/sessions 로그에서 UTF-8로 복구

### TODO (다음 작업)
- [ ] 태블릿 실사용: ⚙ 배틀 연습(듣기→말하기→데미지 흐름), 여행 길, 파트너 HP·물약·꾸미기, 통과 기준(40%) 체감
- [ ] 며칠 뒤 HP 수치(−20/−10/−30)·배틀 확률(3%·하루 1번) 체감 조정
- [ ] [나중] Codex #2·#5: 두 창 동시 실행 시 패배 누적·하루 1번·HP를 DB 트랜잭션 판정으로
- [ ] 📊 대시보드에 레벨/퍼즐/배틀 그래프, Codex 잔여 #7·#11~#14, 드래그 가장자리 자동 스크롤

## 2026-09-15 ~ 09-16 (학습 3층 + 태블릿 피드백, v37~v48)

### 진행 내용
- **방향 전환**: "보상은 이미 8겹으로 포화, 정작 학습 알맹이가 얇다"고 진단. 문장이 한 번 지나가면 끝나고, 단어 패널은 읽기 전용이며, 발음 피드백이 숫자 하나뿐이라는 세 구멍을 우선순위로 제시 → 사용자가 순서대로 승인
- **🔁 복습 — 간격 반복 (v37)**: `js/review.js` 신규. 라이트너 1·2·4·7·14일 → 👑 졸업. 콘텐츠를 열 때 "오늘의 복습 3문장"(건너뛰기 자유, 하루 2번까지 제안), 한 문장 = 한글 뜻 → 🔊 듣기 → 🎤 따라 말하기. 보상은 **복습에서만 나오는 🌟 황금 몬스터볼**(잡힐 확률 2배, 하루 1개) + ❤️ 파트너 회복 — "코인을 더 주는 것"이 아니라 **가장 센 보상(잡기)의 경로를 복습으로 옮기는** 방식. 어뷰징은 3중 차단(푼 문장은 dueAt이 밀려 그날 재출제 불가 / `daily.reviewGolden`을 날짜 단위 전역으로 두어 영화를 바꿔 열어도 1개 / 백업 병합 OR)
- **🎯 말하기 단어별 피드백 (v38)**: `scoreTranscript`에 `hits` 추가 + `wordResults` 신규. 결과 화면에 단어별 ✅/❌, 못 말한 단어를 누르면 **그 구간만 재생**(`playWord`, `wordTimings` 자리와 1:1). 누르는 동안 자동 진행 타이머가 미뤄짐. `track.missedWords` → 📊 "자주 놓치는 단어"
- **🔤 단어 학습 (v40)**: 단어 패널 탭 → 그 단어·표현만 재생(`vocab.findTokenRange`). 복습 회차에 **뜻 4지선다 1문항**, 단어도 문장과 같은 라이트너 규칙(`vocabViews`에 box·dueAt·quizzes)
- **✍️ 받아쓰기 (v42)**: `js/dictation.js` 신규. 복습의 **세 번째 문항 타입**. 빈칸은 **아이가 못 말했던 단어 우선**(`sentenceStats.missed` 재사용), 오답은 `speak.editDistance`로 **비슷하게 들리는 단어**(through → tough·rough·thought). 타이핑 없이 보기에서 고르기
- **/codex 건설적 리뷰 3회** (복습 9건·단어 피드백 6건·단어 학습 8건) → **전부 반영**. 가장 큰 것들: 복습 box 병합이 max라 실패로 내려간 단계를 옛 백업이 되돌리던 버그 / 단어 클릭이 오버레이로 전파돼 **기능이 실기기에서 아예 동작하지 않던 [P1]** / 사전의 "조심해!"와 "조심해"가 함께 보기로 나와 정답을 알고도 틀리던 [P1] / 표현 범위를 첫·끝 단어로 찾아 문장 전체가 잡히던 [P1]
- **태블릿 실사용 피드백 (v43~v48)**: 설정 select가 빈 것처럼 보이던 버그, ⚙ "말하기 결과 보는 시간"(1~5초) 신설, 따라 말하기 기본 ×2.0, **음성 인식이 중간에 끊기던 진짜 원인**, 두 화자 겹친 자막 제외, 장식 위치 자동 계산 + **드래그로 직접 조정**, 연습·복습 뒤 영상 자리 복원, 캐릭터 8마리씩 이어받기, **명단 100마리**, 희귀도 비율 재조정

### 변경 파일
- 신규 `js/review.js`(복습 규칙+UI), `js/dictation.js`(받아쓰기 규칙), `tests/{review,dictation}.test.js`
- `js/{speak,player,track,db,vocab,items,xp,pokemon,puzzle,catch,shop,stats,diag,battle}.js`, `index.html`, `css/style.css`, `sw.js`(v37→v48)
- `tests/{player.logic,speak,speak.flow,db.merge,items,vocab,pokemon,xp,battle}.test.js` — 143개 → **250개**

### 결정사항 / 메모
- **보상 설계 원칙**: 양(코인 더 주기)이 아니라 **종류**로 차별화. 복습에서만 황금 볼이 나오므로 "여기서만 얻는 것"이 생김. 어뷰징은 막되 "복습을 더 하는 것"은 막지 않음(2회차부터 ⚡·💰만)
- **`state.shadowNext`는 기존 반복 신호**('repeat'|'next')였다. 같은 이름으로 타이머 함수를 덮어써 반복 ∞가 깨졌다 → `state.resultDelay`로 분리. 기존 이름을 재사용하기 전에 grep할 것
- **테스트 스텁의 구조적 한계**: `makeEl()._fire('click')`는 **버블링을 모사하지 않아** `stopPropagation` 누락을 못 잡는다. v38이 이 때문에 "테스트 193개 통과"인 채로 실기기에서 동작하지 않았다 → `clickWithBubble` 헬퍼 추가. vm 컨텍스트 배열은 `Array.from()`으로 복사 후 `deepEqual`
- **음성 인식이 끊기던 진짜 원인**(v46): `network`(구글 서버 순간 불안)와 `start-failed`(앞 인식이 덜 정리된 채 start — 안드로이드에서 흔함)가 SR_FATAL에 있어 **한 번만 나도 즉시 폴백**. 게다가 `no-speech`(아이가 말을 안 함)까지 실패로 셌다. → SR_FATAL은 권한·마이크·서비스만, 나머지는 연속 3회, `no-speech`는 제외, 폴백 대기 3분 → 1분. 다음을 위해 `speakLog()`(최근 12회)를 ⚙ 진단에 노출
- **장식 위치**: 모든 포켓몬이 같은 좌표(가로 중앙·위 -16%)를 써서 라프라스는 나비가 뜨고 파이리는 선글라스가 얼굴을 덮었다. `headAnchor`가 그림의 불투명 픽셀 최상단 + 아래 15% 구간의 가로 중심을 계산(6%면 피카츄 귀 한쪽만 잡혀 0.232, 15%면 0.473). 그래도 안 맞는 건 **아이가 드래그로 옮겨 `mons[id].gearPos`에 저장**
- **명단 확장 시 반드시**: `RARITY_IDS`(등급) + `battle.TYPE_OF`(타입)를 같이 채울 것. 빠지면 조용히 보통·노말이 된다 → 테스트로 고정(타입 누락 0, 비율 흔함>보통>희귀>전설, 전설 12% 이하)
- 포켓몬 한글 이름은 **PokeAPI 한국어 공식 이름으로 대조**했다(기억으로 적었으면 386을 "데오키시스"로 틀렸을 것 — 공식은 "테오키스")
- Codex 프롬프트는 40~50KB로 유지. 90KB는 330초 안에 응답 생성 전 타임아웃

### TODO (다음 작업)
- [ ] 태블릿 실사용: 복습이 실제로 뜨는지(오늘 배운 문장이 내일 due), 받아쓰기 보기 4개가 소리 없이 눈으로 구별되는지, 단어 패널 탭 감도, 장식 드래그 조작감
- [ ] ⚙ 진단의 "최근 말하기 기록"으로 인식이 또 끊기면 원인 확인 (🎤 / 🔇network 같은 형태)
- [ ] 며칠 뒤 조정: 복습 간격(1·2·4·7·14일)이 초4에게 맞는지, 빈칸 2개가 과한지, 말하기 결과 3초가 적당한지
- [ ] [나중] Codex 미반영: 두 창 동시 실행 시 황금 볼·HP·패배 경쟁을 DB 트랜잭션 판정으로 / 화면 꺼짐 중 복습 재시도 타이머 / 복습 회차 Promise 소유권(이전 회차가 새 회차의 speakStop을 지움)
- [ ] 제안했던 나머지: 👨‍👩‍👦 부모→아이 한 줄 메시지(대시보드에서 적으면 아이 화면에 표시), 🌙 마무리 화면(오늘 배운 문장 3개 회상)
- [ ] 📊 대시보드에 레벨·퍼즐·복습 그래프

## 2026-09-16 ~ 09-17 (종료 버튼·에세이·아빠 교정 왕복·포켓몬 게임 확장, v49~v59)

### 진행 내용
- **🚪 종료 버튼 (v49)**: 시작 화면 맨 아래 → 인사 화면(오늘 요약)에서 한 번 더 확인 → `window.close()`. 설치한 PWA는 닫히지만 **브라우저 탭은 close()를 무시**하므로 0.5초 뒤 "홈 버튼으로 나가면 돼요"로 전환
- **포켓몬 영상 2편 자막 제작**: 자막 없는 유튜브 영상(Gengar 14분33초 180문장 / Ash&Pikachu 8분7초 86문장)을 faster-whisper로 인식 → 교정 → 한글 직접 번역. 애매한 27개 구간은 **그 구간만 다시 인식해 대조**. 단어장 +37(1,831→1,868)
- **✍️ 에세이 (v50, 엄마 요청)**: 30분 학습하면 오늘 배운 문장 3개의 뒤를 비워 주고 아이가 자기 이야기로 채움 → **규칙 기반 오프라인 교정** → 위(내가 쓴 글)/아래(신코치가 고친 글) → 따라 읽기. `js/essay.js` 신규
- **/codex 건설적 리뷰 (P1 6 · P2 6) 중 11건 반영** — 과교정을 전부 걷어냄(아래 결정사항)
- **👨‍👩‍👦 아빠 교정 왕복 (v51~v53)**: 규칙으로 못 고치는 어순을 사람이 메운다. 📊에서 아이 글을 복사/사진 → Claude가 교정 → 앱에 되돌림. 태블릿에 메신저가 없어 **교정문을 `coach/fixes.json`에 적어 배포로 전달**하는 길까지 만듦(태블릿에서 입력 0)
- **⚔️ 배틀 확률 3% → 4% (v54)**
- **⭐ 등급 옮기기 (v55·v57)**: 아이가 도감에서 신청 → 📊에서 부모가 승인. 못 잡은 포켓몬도 신청 가능, 일괄 승인
- **🎤 한 번 더 읽기 (v56)**: 통과해도 틀린 단어가 표시된 문장을 보며 한 번 더 정확히 읽기
- **⭐ 메가진화·거다이맥스 (v58)**: 🔑 키스톤 600 + 💠 메가스톤 600 / 🍄 다이버섯 10개 → 🍲 다이스프. 배틀에서 메가 ×1.4, 거다이맥스 ×1.6(3턴). 명단 30마리가 변신
- **🔴 볼 등급 (v59)**: 슈퍼볼 25(×1.5)·하이퍼볼 60(×2)·마스터볼 1,200(확정). 잡기 화면이 볼 고르기로

### 변경 파일
- 신규 `js/essay.js`(에세이 규칙+UI), `coach/fixes.json`+`coach/README.md`(배포로 전달하는 교정문), `tests/essay.test.js`
- `js/{player,xp,items,pokemon,shop,stats,catch,battle,track,db,app}.js`, `index.html`, `css/style.css`, `tools/whisper_srt.py`, `vocab/words.json`, `sw.js`(v48→v59)
- `tests/{player.logic,items,xp,battle,stats,track,essay}.test.js` — 250개 → **289개**

### 결정사항 / 메모
- **규칙 교정의 제1원칙은 "과교정 금지"**: 편집거리로 비슷한 사전 단어를 찾으면 **맞게 쓴 말을 망친다**(soccer→sorcery, goat→goal, **ducks→duck**). 명시적 오타 목록 + 붙은 두 글자 자리바꿈만 남기고, `vocab.stemWord`로 변화형은 항상 보존. 3인칭 s 규칙은 통째로 제거(`it go away`를 망침) — **못 고치는 것보다 맞는 문장을 망치는 게 나쁘다**
- **헤드리스 검증을 건너뛰지 말 것**: 에세이에서 `sfx`를 함수처럼 불러 **제출 버튼이 통째로 죽어 있었는데 테스트 268개는 통과**했다. 오버레이를 스텁으로 바꾸는 테스트는 UI 배선을 검증하지 못한다
- **`res.ratio`는 없는 필드**였다 — `runSpeakCheck`는 `passed`/`score.ratio`를 준다. 이 때문에 성공 효과음이 한 번도 안 울렸다
- **연습 반복은 통계에 넣지 않는다**: 🎤 한 번 더 읽기는 XP·시도·통과 횟수를 안 올리고 bestRatio만 갱신. 통계가 부풀면 부모가 실력을 잘못 읽는다
- **아이 권한 설계**: 등급 옮기기는 곧 잡기 확률이라 **아이는 신청, 부모가 승인**. 반대로 에세이·복습은 아이가 자유롭게
- **경제 기준**: 하루 ≈ 100~130코인. 장식 30~150 / 볼 25~60 / **큰 목표 1,200**(메가 세트·마스터볼) ≈ 열흘 치
- **PokeAPI를 사실 확인에 쓸 것**: 한국어 공식명·변신 폼 id·아이템 설명. 거다이맥스는 "스톤"이 아니라 **다이버섯→다이스프**가 맞다
- **Codex는 영어로 답하게 할 것**: 한글 응답이 Windows CP949 파서에서 깨진다. 영어로 받아 번역하면 세션 로그 복구가 필요 없다
- heredoc은 여전히 백슬래시를 먹는다 — `split(/\r?\n/)`가 실제 개행으로 깨져 파일이 죽었다. **코드는 Write/Edit로**

### TODO (다음 작업)
- [ ] 태블릿 실사용: ✍️ 에세이(초4가 영어 2~5단어 타이핑하는 부담, TTS 영어 음성 유무), 🎤 한 번 더 읽기의 시간 체감, ⭐ 등급 신청·일괄 승인, 🔴 볼 고르기
- [ ] 첫 에세이 교정 왕복 해보기 (📊 사진 → `[번호]` 교정 → `coach/fixes.json` 배포)
- [ ] 며칠 뒤 조정: 메가 세트·마스터볼 1,200이 초4에게 너무 먼지, 🍄 다이버섯 하루 2개가 적당한지, 배틀 4% 체감
- [ ] 진우에게 확인: `라이츄-메가X/Y`·`개굴닌자-메가`가 실제로 있는 폼인지 (PokeAPI에는 있으나 내 지식 기준일 이후 자료)
- [ ] `Touch it, Pikachu!`(Ash 편 1분22초) 실제로 그렇게 들리는지 확인
- [ ] [나중] 두 창 동시 실행 경쟁(황금 볼·HP·패배·에세이 완료 플래그)을 DB 트랜잭션 판정으로 한꺼번에
- [ ] 📊 대시보드에 레벨·퍼즐·복습 그래프 / 🌙 마무리 화면

## 2026-09-17 (🪟 두 창 동시 실행 경쟁 정리, v60)

### 진행 내용
- 백로그에 세 세션째 남아 있던 "두 창 동시 실행 시 황금 볼·HP·패배·에세이 플래그 경쟁"을 **원인이 하나라 한꺼번에** 정리. 뿌리는 `putDaily(t.daily)`처럼 **밖에서 읽어둔 사본으로 통째로 저장**하는 것
- **실제 피해를 브라우저에서 재현**: 예전 방식으로 두 창이 30초·20초를 저장하면 결과가 **20초**(30초 증발), 문장 하나 사라짐, **황금 볼 받은 표시가 false로 풀림**(하루 한 번이 무력화). 보상 중복보다 **공부 기록 소실**이 더 큰 피해였다
- `db.js`에 원자 연산 6종: `applyDailyDelta`(증분) · `claimDailyFlag`/`claimDailyCount`(하루 한 번·N번 선점) · `applyHpChange`(바뀐 만큼 + 클램프 + 물약 소모) · `applyBattleLoss`(누적과 "3번이면 잃음" 판정을 함께) · `applyPurchase`(살 수 있는지 판정) + `editDaily`
- `track.js`: `t.daily`는 보기용, 저장은 `t.dailyDelta`(늘어난 만큼)만. `mark*` 6개가 `Promise<선점 성공 여부>` → `player.js`는 **이긴 창에서만** 보상 지급
- 상점 구매도 같은 구멍이 있었다(두 창에서 1,200코인으로 마스터볼 2개) → `buyItem`·`equipMega`·`makeSoup`가 `applyPurchase`를 쓰고 비동기가 됨
- 부모가 📊에서 교정문을 붙이는 `applyEssayFixes`·`markEssayRead`도 아이 기록을 덮어쓰고 있었다 → `editDaily`로

### 변경 파일
- `js/{db,track,player,xp,shop,review,essay}.js`, `sw.js`(v59→v60)
- `tests/{track,player.logic,items,battle,db.merge}.test.js` — 289개 → **296개**

### 결정사항 / 메모
- **원리 하나**: 판정과 계산을 **트랜잭션 안으로**. IndexedDB는 같은 스토어의 readwrite 트랜잭션을 직렬화하므로, 트랜잭션 안에서 읽고 계산하면 그게 곧 잠금이다
- 트랜잭션 안의 `await`는 **IDB 이벤트에서 resolve되는 Promise일 때만** 살아 있다 (마이크로태스크 안이라 트랜잭션이 아직 활성). 다른 Promise를 기다리면 트랜잭션이 닫힌다
- **`putDaily`/`putProfile`은 앱 코드에서 쓰지 말 것** — 주석으로 못 박음. 백업 가져오기·시험용만
- `mark*`가 Promise를 주므로 **보상은 반드시 `.then(won => ...)` 안에서**. 기존 테스트가 sync라 `await tick()`을 넣어야 했다 — 이 규칙을 어기면 테스트에서 바로 드러난다
- 테스트 스텁의 `daily`를 **날짜별 Map**으로 바꿔, 통째로 덮어쓰는 코드가 다시 들어오면 깨지게 했다
- 헤드리스에서 진짜 IndexedDB로 19개 항목 확인 + **상점 버튼을 실제로 눌러** 확인 (v50에서 "테스트 268개 통과인데 버튼이 죽어 있던" 교훈)

### TODO (다음 작업)
- [ ] 배포 후 태블릿에서 하루 치 학습 — 기록이 정상으로 쌓이는지 (저장 구조가 바뀐 곳)
- [ ] 이전 세션 TODO 그대로: ✍️ 에세이·🎤 한 번 더 읽기·⭐ 등급 신청·🔴 볼 고르기 실사용, 첫 에세이 교정 왕복, 진우에게 메가 폼 확인
- [ ] 📊 대시보드에 레벨·퍼즐·복습 그래프 / 🌙 마무리 화면

## 2026-09-17 (이어서 — 비밀번호·아빠 교정 배포·퍼즐·복습 버그·🎟️ 교환권, v61~v67)

### 진행 내용
- **설정 비밀번호 0715 → 0917**: `SETTINGS_PIN_HASH`(SHA-256)와 **구형 브라우저 폴백(뒤집은 문자열)** 두 곳. 폴백을 빼먹으면 `crypto.subtle`이 없는 브라우저에서 옛 비번이 그대로 통한다
- **👨‍👩‍👦 첫 아빠 교정 왕복 실행**: 📊 사진 3문장을 교정해 `coach/fixes.json`으로 배포. `infront→in front` / `I know right?He's→I know, right? He's` / `My musical instrument **is** in my hand`(동사 누락 — 규칙으로는 못 잡는 딱 그 경우). 최소한만 고쳐 아이가 고른 말은 그대로 뒀다
- **🧩 퍼즐 5단어 이상만 (v62)**: `Use Fire Punch!` 같은 3~4단어 감탄·명령 조각이 퍼즐로 나왔다(아버님 지적). 실제 자막 5,527문장으로 재 보니 후보는 54%→32%로 줄지만, 10문장에 한 번이라 **건너뛰는 차례는 젠가 0%·애쉬 0%·토이스토리 1%**
- **🔁 복습 버그 2건 + 가로 드래그 (v63)**: ① `speakStop`을 회차 확인 **전에** 지워 새 회차의 `다 말했어요 ✓`가 죽던 것(되돌려 재현: 고치기 전 `stop=[]`) ② `sleep()` 6곳에 `document.hidden` 가드가 없어 잠긴 태블릿에서 혼자 넘어가던 것 ③ 가로 모드에서 `touch-action:none` 때문에 조각을 정답 칸까지 못 옮기던 것 → 가장자리 자동 스크롤
- **✍️ 에세이 10단어 이상·쓰는 양 4~8단어 (v64)**: 진우 글이 2단어뿐이었다. **문장만 길게 하면 고정 부분만 길어진다**는 걸 짚고 `MAX_BLANK_WORDS`도 함께 올림. `It's really me! Yes! My goodness...`처럼 **네 문장이 합쳐진 큐**를 `isMultiSentence`로 제외(9단어라 길이로는 안 걸린다)
- **🎟️ 다음 영상 교환권 (v65~v66)**: 포켓몬 영상을 일부러 안 넣어 두신 것을 **보이게 잠그고 벌어서 열게**. 조건 셋(코인 4,000 + 전체 문장 80% + 복습 통과 60). 표지는 장면 캡처가 아니라 **나오는 포켓몬 그림 + 실제 대사 한 줄**(저작물을 저장소에 안 두는 원칙)
- **💰 코인 누수 발견·수정**: ◀▶로 문장을 옮겼다 오면 `resetSentenceState`가 말하기 확인을 다시 켜서 **코인이 무제한**이었다. 코드상 정상 수입은 하루 294~824
- **/simplify 4-에이전트 리뷰**: `sentenceStats`가 없애려던 패턴에 그대로 남아 복습 진도를 잃던 것, 프로필 규칙이 트랜잭션에 갇혀 **테스트가 폴백만 타던 것**(items 11·battle 5건 조용히 삼켜짐) 등 12건 반영, 앱 코드 58줄 순감소
- **/codex 건설적 리뷰 → P1 4건 수정 (v67)**: 아래 결정사항 참조

### 변경 파일
- 신규 `js/unlock.js`(교환권 규칙+카탈로그), `tests/unlock.test.js`
- `js/{db,track,xp,player,review,puzzle,essay,library,stats,pokemon,shop,srt,app}.js`, `css/style.css`, `index.html`, `coach/fixes.json`, `sw.js`(v60→v67)
- `tests/{db.merge,track,player.logic,items,battle,puzzle,essay,srt,unlock}.test.js` — 296개 → **318개**

### 결정사항 / 메모
- **[Codex P1] 영상을 지우면 조건이 저절로 채워졌다** — `deleteItem`은 `sentenceStats`를 안 지운다. 분모는 현재 영상, 분자는 전체 기록이라 어려운 영화를 지우면 그 기록이 분자에 남고 분모에서만 빠진다. **기능의 목적이 삭제 한 번으로 무효.** `unlockState({ itemIds })`로 지금 가진 영상만 센다 (105/80 ready=true → 5/80 ready=false)
- **[Codex P1] 분모가 플레이어와 규칙이 달랐다** — 예고는 항상 합치고 길이 밖 큐도 셌다. `srt.countPlayableCues` 하나로 통일(stats.js도 같이 씀)
- **[Codex P1] 공부하고 돌아와도 숫자가 그대로** — `closePlayer`가 저장을 await 안 하고 `showView`했고 `showView`는 목록을 안 그렸다. 아이에게 가장 실망스러운 평범한 버그
- **[Codex P1] 버튼을 억지로 켜면 조건을 건너뛰었다** — 가격은 카탈로그에서, 중복은 거절, 조건은 `verify()`로 **구매 경로 안에서** 재확인
- **저작물 원칙을 지킨 선택**: 애니 장면 캡처를 커밋하면 "닌텐도 저작물은 공개 저장소에 두지 않는다"가 깨진다(GitHub Pages 무료는 Public만). `pokemon.ensureArt`로 **명단 밖 포켓몬 그림도 PokeAPI에서 받아 기기에만** 둔다. ROSTER 밖 id는 `loadCharacters`가 걸러 도감·퍼즐엔 안 나온다
- **테스트 스텁이 수정을 검증 못 하는 경우** — Codex가 짚었다: `claimSpeakReward: () => true` 스텁 때문에 코인 누수 수정이 테스트되지 않았다. 회귀 테스트는 **순수 함수 쪽에 직접** 붙일 것
- **에세이 끊을 자리**: 뒤에서부터 훑으면 늘 빈칸이 최소가 된다 → 범위 **가운데**에 가장 가까운 자리를 고른다(실측 분포가 6단어에 몰림)
- **다음 방향 결정**: 🔢 수학을 **이 앱의 두 번째 과목**으로 (별도 앱 아님 — 도감·코인을 공유해야 "수학을 해서 마스터볼을 산다"가 성립). 수학은 문제 생성·채점이 자동이라 shincoach에서 제일 힘들었던 두 병목이 없다. 국어는 정답이 하나가 아니라 가장 어렵다(에세이에서 이미 겪음)

### TODO (다음 작업)
- [ ] **태블릿 실사용 — 이게 지금 가장 값어치 있다.** v49~v67(19개 버전)이 실기기에서 거의 검증되지 않았다. 특히 ✍️ 에세이의 🔊 TTS 영어 음성 유무, 어제 배포한 아빠 교정 3문장이 뜨는지, 가로 모드 퍼즐 드래그
- [ ] 설정 비밀번호가 **0917**로 바뀐 것 확인 (0715는 막힘)
- [ ] 🎟️ 교환권 조건(4,000 / 80% / 60개)이 진우에게 너무 먼지 며칠 보고 조정 — `js/unlock.js` 상수 세 개
- [ ] 📊 부모용 추세 화면 (주 단위 말하기 통과율·퍼즐 정답률·⭐정복 추이) — 데이터는 이미 다 쌓여 있고 그리기만 하면 됨
- [ ] [나중] 🔢 수학 과목 추가 — **태블릿 검증이 끝난 뒤에** 시작
- [ ] [Codex P2 미반영] 교환권 배달 취소·환불, 제목이 다를 때 부모가 연결하는 기능, 조건이 구조적으로 불가능할 때 안내, 포스터가 `artUrl`을 안 읽는 것

## 2026-09-17~18 (실기기 신고 대응 이틀, v68~v82)

### 진행 내용
아버님이 태블릿에서 진우를 지켜보며 준 신고를 하나씩 받아 고쳤다. **재현 → 원인 확정 → 고침 → 회귀 테스트** 순서를 지켰고, 추측으로 넘어간 것은 없다.

- **🚑 저장 깨짐 (v68)**: "저장소를 열 수 없어요: Data lost due to missing file"로 앱이 멈춤. 별도 Chrome 프로필로 IndexedDB 곁 파일을 지워 가며 재현 → **Chrome은 64KB가 넘는 값을 곁 파일로 뺀다**(30KB·60KB 인라인, 70KB부터 파일). `items` 레코드는 자막 전문이라 거의 항상 넘고, 그 파일이 사라지면 `items.getAll()`이 **통째로** 실패한다. `getAllKeys` + 한 건씩 읽기로 바꾸고 깨진 것만 `broken`으로 표시. 깨진 영상의 제목은 `sessions.title`에서 되찾음. **아이 기록(profile·daily·sentenceStats)은 작아서 멀쩡했다**
- **🛟 기록 사본 (v70)**: 코인·도감·복습 진도를 localStorage에 자동 사본. 형식을 📊 "가져오기"와 같게 만들어 복구에 병합 규칙을 그대로 씀. 복구는 부모가 📊에서 결정
- **🧩🎀 드래그 먹통 (v70)**: 진우가 "퍼즐이 아무 반응이 없어 앱을 껐다 켜야 한다". 드래그 중 `pointerup`이 안 오면(안드로이드는 화면이 꺼지면 안 준다) `ui.drag`가 남아 이후 모든 터치가 무시됐다. 꾸미기 장식 드래그도 같은 구멍(옛 코드로 되돌려 확인하니 장식이 손가락을 계속 따라다녔다)
- **🎟️ 예고·🎒 도감 중복 (v71·v72)**: "팬텀 광고 문구가 두 번" — 화면을 지우고 `await` 하는 함수에 요청 번호가 없었다. `main()`이 `showView('library')`와 `initLibrary()`로 **매번 두 번** 그렸다. 도감도 같은 모양(🎒를 빠르게 두 번 누르면 내용이 두 배)
- **⚔️ 배틀 8%·하루 3번 (v73·v74)**: "하루 종일 안 나왔다" → 4%에서 하루 30문장이면 못 만날 확률 35%. 계산으로 확인하고 올림
- **🎤 말하기 인식 (v75·v80)**: ① 안드로이드 인식기는 말 시작 전 침묵에서 스스로 끝나는데 그걸로 판정해 버려, 아이가 문장을 읽고 숨 고르는 사이 끝나면 "말소리를 못 들었어요"가 떴다 → 같은 문장에서 2번까지 다시 듣기 ② `prepareMic()` 대기 중 `releaseMic()`이 불리면 늦게 온 스트림이 인식을 막았다 → 세대 번호 ③ **`state.speakUnavailable`이 켜지기만 하고 꺼지는 데가 없었다** — 마이크를 한 번만 못 잡아도 세션 내내 말하기 확인이 사라져 앱을 껐다 켜야 했다 → 시각으로 바꿔 3분 뒤 자동 재시도
- **🔁 복습 (v76)**: 영상 열 때 한 번만 묻고 2번 거절하면 그날 끝이라 밀렸다 → 학습 중 20% + 8문장 쿨다운. ❤️15를 매번 주도록(전에는 첫 회차만)
- **🎟️ 조건을 비율 → 개수 (v77·v78·v79)**: "진도가 안 는 것 같다"가 맞았다. 5,527문장의 80% = 4,422문장이라 하루 40문장을 해도 막대가 0.9%. **영상을 넣으면 목표가 더 멀어지는** 구조이기도 했다. `NEED.doneSentences`(1,000)로 바꾸고 코인 2,000·복습 50으로 조정
- **🎵 이벤트 BGM (v81)**: 퍼즐·배틀·잡기에 8비트 합성 음악. **끝난 자리를 기억해 다음 이벤트에서 이어서**. 말하기 중에는 멈춤
- **🗣 미니언즈어·🔓 자유 이동·🟡 하이퍼볼 (v82)**: 영어가 아닌 대사는 따라 말하라고 안 하고(실측 미니언즈 12%), ⏭ 넘기기 버튼을 넣고, 이미 끝낸 문장으로는 자유롭게 이동하게 하고, 하이퍼볼을 600으로
- **📚 포켓몬 영상 3편**: prime_suspect 65문장 · wild2 46 · iconic 103. 중복 0% 확인 후 제작. 단어장 1,868 → 1,928
- **🎬 모아나 2.24GB**: 이미 H.264인데 태블릿이 거부 → **2GB 초과**가 원인. 1.06GB로 변환

### 변경 파일
- 신규 `js/bgm.js`, `js/backup.js`, `tools/{dupcheck.mjs,align_srt.py,verify_lines.py}`, `tests/backup.test.js`
- `js/{db,track,player,speak,puzzle,shop,library,stats,unlock,vocab,items,sfx,pokedex,battle,catch,app}.js`, `css/style.css`, `index.html`, `vocab/{words,phrases}.json`, `tools/check.mjs`, `sw.js`(v67→v82)
- 테스트 318개 → **350개**

### 결정사항 / 메모
- **"앱을 껐다 켜면 낫는다"는 신고는 모듈 전역 플래그부터 본다** — 이틀에 세 번 같은 모양이었다(퍼즐 `ui.drag`, 말하기 `speakUnavailable`, 예방으로 BGM). 켜는 곳과 끄는 곳의 수를 세면 바로 나온다. 고칠 때는 **시각으로 바꿔 자동 복구**하거나 **다음 사용자 동작이 되돌리게** 한다
- **화면을 지우고 await 하는 함수에는 요청 번호가 필요하다** — 예고·도감 중복이 모두 이 모양이었다. `renderStats`는 await **뒤에** 한 번 더 비워서 무사했다
- **조건은 비율보다 개수** — 비율은 분모가 커지면 아이가 공부해도 진도가 안 늘고, 콘텐츠를 늘려 줄수록 목표가 멀어진다. 개수로 바꾸니 "지워서 조건 채우기" 구멍(Codex P1)도 구조적으로 사라졌다
- **판정 기준은 실제 데이터로 잰다** — 영어 여부 판정을 처음엔 사전 적중률만으로 했다가 `James and Henry!`·`But thankfully Ed intervened,` 같은 멀쩡한 영어가 걸러졌다. 이름(문장 중간 대문자)을 빼고 영어다운 꼬리를 인정한 뒤 **실제 자막 세 편으로 재서** 확정(미니언즈 12% · 토이스토리5 2% · 젠가 20%)
- **단어장에 일부러 넣은 외국어가 판정을 흔들었다** — `bello`가 "안녕!(미니언 말)"로 사전에 있어 영어로 세어졌다. 같은 표시가 47개 → `vocab.foreign`으로 분리. 단어 패널에는 그대로 둔다
- **`tools/check.mjs`의 APP_SHELL 가드가 실제로 일했다** — `js/bgm.js`를 sw.js에 안 넣은 것을 바로 잡아냈다(안 잡혔으면 오프라인에서 BGM만 조용히 죽었을 것)
- **2GB는 안드로이드 Chrome의 실질 한계** — 코덱이 멀쩡해도 안 열린다. 가져오기 안내도 크기를 짚도록 고쳤다
- ffmpeg는 `-map`으로 영상·소리만 골라도 **챕터 트랙을 따라 붙인다** → `-map_chapters -1`

### TODO (다음 작업)
- [ ] **태블릿 실사용 관찰 후 수치 조정** — 🎟️ 1,000문장이 적당한지(📊 "콘텐츠별 진행"의 합으로 확인), 🔁 복습 20%가 잦은지, 🎵 BGM 분위기·볼륨, 🗣 판정 기준 0.4
- [ ] 새 영상 3편(prime_suspect·wild2·iconic)을 태블릿에 넣기 — 이제 목표가 안 밀린다
- [ ] 모아나는 `F:/per/mp3/moana_tablet.mp4`(1.06GB)로 교체해 넣기
- [ ] ⚔️ 배틀 하루 3번이 되면서 **한 포켓몬으로 하루에 3번 져서 잃을 수도** 있다 — 진우가 속상해하면 `lossesToLose`를 올리거나 하루 패배 상한
- [ ] 📊 부모용 추세 화면 (데이터는 이미 쌓여 있음)
- [ ] [나중] 🔢 수학 과목 — 태블릿 검증이 끝난 뒤에
